# 二重螺旋脚本管理后台开发技术文档（v2 契约修订版）

> **本文档是两个项目之间的接口契约。** 后续开发时，如果字段、路径或数据含义发生变化，必须先更新本文档并同步更新 OpenAPI 快照（`docs/api/`），再修改代码。
>
> | 项目 | 内容 |
> |---|---|
> | 文档版本 | v2（契约修订版） |
> | 修订日期 | 2026-09-12 |
> | 上一版 | `docs/archive/后台开发技术文档.v1.md` |
> | 契约快照 | `docs/api/openapi-v1.yaml`（客户端接口）、`docs/api/openapi-admin.yaml`（管理后台接口） |
> | 修订依据 | AScript 脚本项目实际代码（`remote_loader.py`、`dist/latest.json`、`runtime_entry.py`、`res/task/AppGame.py`），脚本版本 `VERSION = 2.0.0.8`；关键 SQL/约束已在 SQLite 3.53 实测（见附录 B） |
>
> 本版修订的动机：v1 是一份高质量的规划文档，但有 3 处会在开工后返工——**（1）`/releases/latest` 的字段结构与现网设备真正在校验的清单结构不一致；（2）验收标准混入了脚本端条目，后台无法自证完成；（3）时长与会话的关键规则（结束时间、幂等、并发）只给了原则没定死。** 本版把这些全部落定，并补齐部署、安全、保留策略等实现细节。

---

## 0. v2 修订摘要

| 编号 | 级别 | 变更 | 原因 / 依据 |
|---|---|---|---|
| C1 | P0 | `/api/v1/releases/latest` 改为现有 `latest.json` 的**超集**，并新增零改动兼容路径 `/dist/latest.json` | 现网设备对清单做硬校验（`schema=1`、`package.path` 前缀、`sha256`、`size`），照 v1 的扁平结构实现会导致**所有已发布设备更新链路失效** |
| C2 | P0 | 明确客户端清单来源优先级：**后台 → OSS → GitHub Raw → 本地缓存 → 工程内置** | 采纳"后台优先、失败回退 OSS/GitHub"策略；后台故障不得阻断更新 |
| C3 | P0 | 验收标准拆为「后台验收」「脚本接入验收」「部署验收」三张表，并增加"后台阶段完成定义" | v1 第 21 节混入大量脚本端条目，后台无法自证完成 |
| C4 | P1 | 时间统一以 **INTEGER Unix 秒（UTC）** 落库，接口层用 ISO 8601（`Z`）表达 | 排行榜区间切分需要算术与索引，字符串时间在 SQLite 下易错且慢 |
| C5 | P1 | 会话结束时间定死为 `last_heartbeat_at + 120s` 容差 | 客户端定时下线路径直接 `system.exit()`，**多数会话注定以 timeout 结束**，不补容差会系统性少算时长 |
| C6 | P1 | 幂等与并发落到数据库约束：`run_id` 全局唯一、`installation_id` 部分唯一索引、心跳时间 `MAX()` 防回退 | 应用层判断在并发/重试下不可靠 |
| C7 | P1 | `run_id` 格式定死为全局唯一（含 `install_id` 前缀） | v1 示例 `run-20260912-001` 存在跨设备撞车风险 |
| C8 | P1 | `installations`、`runtime_runs` 增加 `release_id` 字段 | 只有 `app_version` 时，"版本使用统计""回滚后谁还在旧包"做不出来 |
| C9 | P1 | 回滚改为**渠道指针表** `release_channels`，不再按 `created_at` 排序取最新 | 补录旧版本发布记录会意外"变成最新" |
| C10 | P1 | `installations` 增加 `update_channel` | v1 支持 stable/beta 但没有设备渠道入口，该字段是死字段 |
| C11 | P1 | 定义 `min_client_version` / `forced` 的语义：服务端计算并返回 `update_required` | 版本比较规则只应在服务端一处实现 |
| C12 | P1 | 定义 `status=disabled` 实例的行为（写接口拒绝、活动会话关闭、排行榜剔除、历史保留） | 否则"禁用实例"是半个功能 |
| C13 | P1 | 发布入库时**服务端自己下载包并校验 `size` + `sha256`**；迁移期双发布 | 挡住"手抖填错哈希导致全网更新失败"这类最贵的错误 |
| C14 | P1 | 排行榜给出 clamp 算法与 SQL，统一口径（一律现算，不读 `duration_seconds`），并定死排序与分页 | v1 只写了"要切分"，没给算法 |
| C15 | P1 | 运维细节落地：uvicorn 固定 1 worker、scheduler 独立进程、WAL、Alembic `render_as_batch`、`VACUUM INTO` 备份 | 这些是 SQLite + Compose 必然踩的坑 |
| C16 | P1 | 管理后台实现方式与安全落定：模板 + htmx、Cookie 会话 + CSRF、输出转义 + CSP | v1 只写了"要有登录"，XSS 与认证方式未定 |
| C17 | P1 | 增加数据保留与容量策略 | `runtime_runs` / 操作日志会无限增长 |
| C18 | P2 | 明确**第一阶段不做** `install_secret` / HMAC，只做限流 + 校验，并在文档中记录残余风险与后续兼容升级路径 | 已确认的产品决策；风险必须显式记录而不是隐去 |
| C19 | P2 | 移除排行榜响应中的 `device_count`（第一阶段恒为 1）；补充错误码 → HTTP 状态的映射、`X-Request-ID` 约定 | 避免前端误解与排障困难 |
| C20 | P2 | 新增「客户端最小接入点」章节与启动路径超时约束（后台请求 ≤ 3 秒） | 后台在脚本启动路径上，慢后台不得让每次启动变慢 |
| C21 | P2 | 管理后台前端落定：Jinja2 模板 + htmx + Bootstrap 5.3 CSS（全部 vendor 入库、不走 CDN、不引 Alpine）；并规定 `api/v1` 与 `admin` 均为薄壳、业务规则只在 `app/services/` 一份 | 无 Node 构建、CSP 保持 `script-src 'self'`；避免页面与 API 两套逻辑漂移 |

> 注：C1/C2/C3 属于**开工前必须落定**的项，本版已完成；C18 是**已知并接受的取舍**，不是遗漏。

---

## 1. 文档目的

本文档用于指导独立后台项目的设计和开发，并作为**两个项目之间唯一的接口契约来源**。

后台项目与 AScript 脚本项目分开维护：

- **后台项目**负责运行时发布管理、脚本安装实例、运行会话、运行时长统计与排行榜、管理员操作；
- **AScript 脚本项目**负责本地游戏自动化逻辑、用户配置界面，以及调用后台接口的客户端模块。

本文档约定的接口，以 `docs/api/` 下的 OpenAPI 快照为机器可读版本。**两者冲突时以本文档 + OpenAPI 快照同时修订后的最新状态为准，禁止只改代码不改契约。**

当前阶段只交付后台，不在本阶段修改 AScript 脚本代码（脚本接入独立立项，见第 4.3 与第 20 节 M6）。

---

## 2. 当前项目背景

当前脚本通过 AScript 导入运行，主要代码位于 `res/` 目录。

脚本已经使用远程运行时更新机制：

```text
AScript 启动脚本
    ↓
remote_loader.py 获取远程版本清单（dist/latest.json）
    ↓
从 OSS 下载运行时 ZIP（失败回退 GitHub Raw）
    ↓
校验 ZIP 精确字节数 + SHA-256
    ↓
解压到按 release_id 分目录的缓存
    ↓
加入 sys.path，导入 erchong_runtime.runtime_entry
    ↓
成功后才写 active.json（断网回退依据）
```

当前发布相关内容：

```text
runtime_entry.py
remote_loader.py
tools/build_runtime_release.py
dist/latest.json
dist/releases/*.zip
res/
```

当前正式 Git 分支为 `main`。以前用于测试在线更新的 `runtime` 分支仅作为下载备用源存在（`remote_loader.py` 中 `RELEASE_BRANCH = "runtime"`），不是后台代码分支。

当前运行包的文件命名（`erchong-runtime-<release_id>.zip`、包目录 `erchong_runtime`）表示"运行时程序"，与 Git 分支无关，保持不变。

---

## 3. 后台项目定位

后台第一阶段定位为：

```text
运行时发布管理
+
脚本安装实例管理
+
运行会话记录
+
运行时长统计
+
运行时长排行榜
+
管理员操作后台
```

后台**不是**收费授权平台，也**不是**用户社区。脚本当前免费使用，因此第一阶段不要求普通用户注册、登录、支付或订阅。

后台必须保留管理员登录，用于保护版本发布、设备管理、排行榜管理和系统配置。

排行榜的正式称呼为「**脚本安装实例运行时长排行榜**」，页面可简称为「运行时长排行榜」。后台页面应始终显示完整 `install_id`。

---

## 4. 第一阶段范围

### 4.1 第一阶段要做的

1. 管理员登录、会话与操作审计；
2. 运行时版本发布管理（创建、校验、启用 / 禁用、渠道指针、回滚）；
3. 兼容清单接口（`/api/v1/releases/latest` 与零改动兼容路径 `/dist/latest.json`）；
4. 脚本安装实例登记与资料（`display_name`）管理；
5. 运行会话开始、心跳、结束；服务端计算运行时长；
6. 超时会话自动关闭；
7. 今日 / 本周 / 本月 / 总榜；
8. 查看当前在线安装实例（按 `last_seen_at` / 活动会话）；
9. Docker Compose 部署（backend + scheduler + 反向代理）；
10. SQLite 数据持久化与自动备份；
11. 基础接口限流、请求体大小限制与操作日志；
12. 健康检查接口。

### 4.2 第一阶段明确不做的

- 普通用户账号登录、用户密码体系；
- 支付、订阅、邀请码、代理；
- 用户私有配置云同步；
- 远程执行任意代码、远程控制云手机；
- 自动上传大量截图、复杂异常日志上报；
- 自动判断游戏画面是否卡死；
- Redis、消息队列、微服务集群、Kubernetes；
- 在 ECS 上直接运行游戏脚本；
- 多设备身份合并与身份迁移；
- 排行榜并列名次、历史榜单快照。

脚本不会主动抛出明确错误，且"卡死检测"实现复杂，因此异常日志上报不作为第一阶段需求。

运行会话的心跳只用于确认脚本仍在运行和计算时长，**不携带任务名称、阶段、轮次、截图或识别结果**。

### 4.3 阶段边界（新增）

| 阶段 | 交付物 | 验收依据 |
|---|---|---|
| 后台阶段（本次） | 后台服务、管理页面、部署编排、契约快照 | 第 21.1、21.3 节 |
| 脚本接入阶段（M6，独立立项） | `install_id` 持久化、客户端后台模块、心跳线程、用户名界面、清单来源切换 | 第 21.2 节 |

**后台阶段完成定义**：接口按 OpenAPI 快照可用、契约测试（`tests/`）全绿、用仓库内的假客户端脚本能完整跑通「注册 → 开始会话 → 心跳 → 停止 / 超时 → 上榜」闭环、`docker compose up -d` 在干净目录可复现启动。满足以上即视为后台阶段完成，**不以"真实设备已接入"为完成条件**。

---

## 5. 事实基线：运行时清单与客户端校验规则（新增，C1 依据）

后台的发布接口必须与设备上**已经存在**的客户端代码兼容。以下是从脚本项目源码核对出的硬约束，属于冻结的兼容面。

### 5.1 现网清单实际结构（`dist/latest.json`，schema=1）

```json
{
  "schema": 1,
  "release_id": "v2.0.0.8-94503c6d0762",
  "app_version": "2.0.0.8",
  "created_at": "2026-09-11T15:40:09+00:00",
  "package": {
    "path": "dist/releases/erchong-runtime-v2.0.0.8-94503c6d0762.zip",
    "sha256": "94503c6d0762a33bcb1e3e0e7f3f597b455a03fbb0a666bc328ad883669c76cb",
    "size": 696613
  }
}
```

### 5.2 客户端校验规则（`remote_loader._validate_manifest` / `_download` / `_extract_package`）

| 字段 | 客户端要求 | 违反后果 |
|---|---|---|
| HTTP 状态 | 必须 2xx（`raise_for_status`） | 该源判定失败，换下一个源 |
| 响应体 | 必须是可解析 JSON 对象 | 同上 |
| `schema` | 必须严格等于 `1` | 抛 `不支持的远程清单格式` |
| `release_id` | 非空；字符集仅 `[A-Za-z0-9-_.]`；**会作为设备缓存目录名** | 抛 `release_id 包含非法字符` |
| `package.path` | 必须以 `dist/releases/` 开头、以 `.zip` 结尾、不含 `..` | 抛 `远程包路径不合法` |
| `package.sha256` | 64 位十六进制（比较时转小写） | 抛 `远程包 SHA-256 不合法` |
| `package.size` | 正整数，且 ≤ **20 MiB**（`MAX_PACKAGE_BYTES = 20 * 1024 * 1024`） | 抛 `远程包大小不合法` |
| 下载 | 连接超时 4 秒 / 读取超时 12 秒；实际字节数必须等于 `size`；SHA-256 必须匹配 | 该源判定失败 |
| 解压 | ZIP 内必须存在 `erchong_runtime/runtime_entry.py`，且成员路径不得越界 | 解压失败，不写 `active.json` |
| 附加查询参数 | 客户端会在清单 URL 后附加 `?v=<每5分钟变化的整数>` 破除中间缓存 | **后台必须忽略未知查询参数**，不得返回 400 |

### 5.3 客户端源顺序与缓存（接入后）

```text
后台清单（超时 ≤ 3 秒，仅启动路径）
    ↓ 失败
OSS 直链 dist/latest.json
    ↓ 失败
GitHub Raw（runtime 分支）
    ↓ 失败
设备本地 active.json 指向的上次成功缓存
    ↓ 无缓存
工程内置版本（保证首次断网也能打开脚本）
```

`active.json`（`{release_id, app_version, sha256}`）只在远程包解压 + Python 导入成功后才更新，因此坏包不会替换可用缓存。后台不得改变这个语义。

---

## 6. 身份模型

### 6.1 install_id

`install_id` 是脚本安装实例的长期随机标识。

必须满足：

- 第一次不存在时生成；
- 生成后保存到持久化配置；
- 后续启动继续复用；
- 重新导入脚本、远程运行时更新、`res/` 更新、重启 AScript 均**不重新生成**；
- 只有用户清除本地持久化数据后才允许生成新 ID。

格式约定（后台校验，客户端生成）：

```text
^[A-Za-z0-9._-]{8,64}$
推荐形式：ER-XXXX-XXXX-XXXX（12 位大写 hex，分三段）
```

**推荐保存位置（脚本接入阶段执行，后台只依赖结果）：**

| 位置 | 是否可用 | 说明 |
|---|---|---|
| AScript `KeyValue` 的**新键**（如 `backend_identity`） | ✅ 推荐 | 与现有 `is_execute_mihan` 同级；可存 `install_id` + `display_name` |
| 缓存根目录下的独立 JSON（与 `update-log-state.json` 同级） | ✅ 可用 | 即 `R.sd("AScript/erchong_runtime")` 下 |
| `KeyValue` 的 `asdata` 键 | ❌ 禁止 | `asdata` 是表单缓存（`form-cache.js` / `runtime_entry.py` 读写），表单重置或缓存损坏会连带丢失身份 |
| 远程运行时解压目录（`releases/<release_id>/`） | ❌ 禁止 | 运行时更新即换目录，身份会随之丢失 |
| 运行时 ZIP 包内 | ❌ 禁止 | 同上 |

### 6.2 run_id

`run_id` 表示一次脚本运行会话。每次启动脚本生成新的 `run_id`，即使 `install_id` 不变也不得复用。

格式约定（**全局唯一**，C7）：

```text
<install_id>-<yyyyMMddTHHmmss>-<6位随机小写字母数字>
^[A-Za-z0-9._-]{16,96}$
示例：ER-7F3A-91C2-8D44-20260912T200000-a3f9c1
```

后台必须拒绝不符合格式或与已有 `run_id` 冲突但归属不同实例的请求。

```text
install_id：长期固定，排行榜按它累计
run_id：每次启动唯一，后台按它记录会话
```

### 6.3 display_name

用户可以设置排行榜显示名称，字段命名为 `display_name`。

显示规则（**只由服务端实现**，客户端不重复实现）：

```text
display_name 不为空 → 显示 display_name
display_name 为空   → 显示 install_id（后端另有掩码配置时可显示短码）
```

`display_name` 不是唯一身份，不能作为数据库主键；改名后历史时长仍归属同一 `install_id`。

校验规则（必须同时适用于输入校验与输出渲染）：

| 规则 | 值 |
|---|---|
| 长度 | 1～20 个字符（按 Unicode 码点计），允许设为空字符串以恢复默认显示 |
| 字符集 | 中文、字母、数字、空格、`_`、`-`、`.`；**禁止** `<` `>` `&` `"` `'` `\` 与所有控制字符 |
| 处理 | 去除首尾空格、压缩连续空格 |
| 保留名 | 禁止 `admin`、`administrator`、`系统`、`官方`、`客服` 等保留名（大小写不敏感） |
| 重名 | 允许 |
| 频率 | 每实例 3 次 / 小时，20 次 / 天，超出返回 429 |
| 渲染 | 管理页面与任何 HTML 输出必须做转义，并配置 CSP（见第 12.4 节） |

### 6.4 多设备规则

第一阶段无普通用户登录，一个 `install_id` 默认代表一个脚本安装实例。同一自然人在多台云手机运行会产生多个 `install_id`。

排行榜按 `install_id` 聚合，不实现多设备合并与身份迁移。以后如需，再增加身份迁移码或管理员合并功能。

---

## 7. 运行时发布契约（重写）

### 7.1 来源优先级（C2）

客户端清单获取顺序：**后台 → OSS → GitHub Raw → 本地缓存 → 工程内置**。

后台是**辅助能力**：后台不可用时，客户端必须仍能完成更新检查并启动脚本。

### 7.2 `GET /api/v1/releases/latest` 响应结构（C1：`latest.json` 超集）

```json
{
  "schema": 1,
  "release_id": "v2.0.0.8-94503c6d0762",
  "app_version": "2.0.0.8",
  "created_at": "2026-09-11T15:40:09+00:00",
  "package": {
    "path": "dist/releases/erchong-runtime-v2.0.0.8-94503c6d0762.zip",
    "sha256": "94503c6d0762a33bcb1e3e0e7f3f597b455a03fbb0a666bc328ad883669c76cb",
    "size": 696613,
    "url": "https://as-erchong.oss-cn-beijing.aliyuncs.com/dist/releases/erchong-runtime-v2.0.0.8-94503c6d0762.zip",
    "mirrors": [
      "https://raw.githubusercontent.com/qingushan/as-erchong/runtime/dist/releases/erchong-runtime-v2.0.0.8-94503c6d0762.zip"
    ]
  },
  "channel": "stable",
  "release_notes": "修复任务识别问题",
  "min_client_version": null,
  "forced": false,
  "update_required": false,
  "manifest_ttl_seconds": 300,
  "api_base": "https://as-erchong-api.example.com",
  "server_time": "2026-09-12T12:30:00Z"
}
```

字段契约：

| 字段 | 必需 | 说明 |
|---|---|---|
| `schema` | ✅ | 固定 `1` |
| `release_id` | ✅ | 由 `tools/build_runtime_release.py` 生成（`v<版本>-<内容哈希前12位>`），**后台只允许录入，禁止自造** |
| `app_version` | ✅ | 与包内 `res/config.py` 的 `VERSION` 一致 |
| `created_at` | ✅ | ISO 8601（UTC，带 `Z` 或 `+00:00`） |
| `package.path` | ✅ | 仓库相对路径，必须以 `dist/releases/` 开头、以 `.zip` 结尾 |
| `package.sha256` | ✅ | 64 位小写 hex |
| `package.size` | ✅ | 精确字节数，必须 ≤ 20 MiB |
| `package.url` | ➖ | 绝对下载地址（`package_base_url + path`）；客户端当前版本不读取，供后续与第三方使用 |
| `package.mirrors` | ➖ | 备用绝对地址数组 |
| `channel` | ➖ | `stable` / `beta` |
| `release_notes` | ➖ | 管理员填写的更新说明 |
| `min_client_version` | ➖ | 低于该版本必须更新；`null` 表示不限制 |
| `forced` | ➖ | 是否强制更新（与 `min_client_version` 一起决定 `update_required`） |
| `update_required` | ➖ | **服务端算好的结论**，客户端不自行比较版本 |
| `manifest_ttl_seconds` | ➖ | 客户端本地缓存该清单的秒数，默认 300 |
| `api_base` | ➖ | 后台基址，供未来域名迁移使用（客户端后续版本读取，当前忽略） |
| `server_time` | ➖ | 服务端当前时间，用于客户端校正本地时钟 |

**未知字段必须被忽略**，客户端不做严格模式解析。

查询参数：

```text
GET /api/v1/releases/latest?channel=stable|beta&install_id=ER-...&app_version=2.0.0.8&v=12345678
```

- `channel` 省略且提供了 `install_id` 时，按该实例的 `update_channel` 解析；
- 未提供任何渠道信息时默认 `stable`；
- 未知查询参数（如客户端附加的 `v=`）必须忽略；
- 无可用发布记录时返回 404 `NOT_FOUND`。

HTTP 缓存：响应带 `ETag: "<release_id>"` 与 `Cache-Control: public, max-age=60`；命中 `If-None-Match` 返回 304。

### 7.3 零改动兼容路径（推荐接入方式，C20）

客户端 `remote_loader.py` 的清单路径常量是 `MANIFEST_PATH = "dist/latest.json"`，ZIP 路径是 `dist/releases/*.zip`，两者都相对于"源基址"。因此后台**额外**提供两个兼容路径：

```text
GET /dist/latest.json          → 返回与 7.2 完全相同的响应体（严格 latest.json 结构 + 扩展字段）
GET /dist/releases/{filename}  → 302 重定向到 OSS 上的同名对象
```

这样脚本接入阶段只需要在 `SOURCE_BASES` 前面插入一个后台基址：

```python
SOURCE_BASES = (
    "https://as-erchong-api.example.com/",   # 后台优先
    "https://as-erchong.oss-cn-beijing.aliyuncs.com/",
    "https://raw.githubusercontent.com/{repo}/{branch}/",
)
```

**校验逻辑、下载逻辑、缓存逻辑、回退逻辑全部不用改**（`requests` 默认跟随 302，最终响应头与字节流来自 OSS，`size` / `sha256` 校验照常生效）。

同时保留规范接口 `GET /api/v1/releases/latest`，供管理页面、诊断工具和未来的客户端版本使用。

### 7.4 客户端启动路径超时约束（C20）

后台清单请求位于脚本**启动路径**上。若按常规 4s / 12s 超时，后台故障会让每次启动多等十几秒。

约定：

```text
启动路径请求后台清单：连接超时 2 秒 / 读取超时 3 秒，不重试，失败立即回退 OSS
非启动路径（注册、会话、心跳）：连接超时 5 秒 / 读取超时 10 秒，重试 1 次
```

客户端还应把最近一次成功清单缓存在本地（`manifest_ttl_seconds` 内直接使用），避免每次启动都打后台。

### 7.5 发布入库与校验（C13）

管理员创建发布记录时填写：`release_id`、`app_version`、`channel`、`package_path`、`package_base_url`、`package_sha256`、`package_size`、`release_notes`、`min_client_version`、`forced`。

**服务端必须自行校验后才允许启用：**

```text
1. 格式校验：release_id / package_path / sha256 / size 满足第 5.2 节全部规则
   （尤其 size ≤ 20 MiB，与客户端上限一致）
2. 拉取校验：后台按 package_base_url + package_path 下载，核对真实字节数与 SHA-256
3. 状态机：verified_status = unverified → ok / failed
4. 只有 verified_status = ok 的记录才能 enabled = 1、才能被渠道指针指向
```

校验失败的记录保留在库中（便于排查），但不得启用，且不能在 `latest` 中返回。

管理页面提供"重新校验"操作；校验任务可以复用 scheduler 进程，不必引入队列。

### 7.6 渠道与回滚（C9）

新增渠道指针表 `release_channels`：`channel`（主键）→ `release_id`。

```text
GET /api/v1/releases/latest 读取渠道指针，而不是"按 created_at 取最新"
回滚 = 把指针指回上一个已验证版本 + 禁用出问题的版本（单事务 + 审计日志）
指针必须指向 verified_status = ok 且 enabled = 1 的发布记录，否则拒绝
```

因此补录历史发布记录不会意外改变线上版本。每个渠道的最近 5 次指针变更记入操作日志。

### 7.7 迁移期双发布

老设备只认 OSS 的 `dist/latest.json`。因此在脚本端完成接入（M6）之前：

```text
同一 release_id 必须同时存在两处：
  1. OSS:   dist/latest.json + dist/releases/<zip>       ← 老设备
  2. 后台:  runtime_releases 记录 + 渠道指针               ← 新设备/管理页面
```

建议在 `tools/build_runtime_release.py` 增加 `--register` 选项：构建完成后调用后台管理接口写入发布记录并触发校验，用管理员 token 鉴权（token 只从环境变量读取，禁止写入仓库或运行时 ZIP）。

两处 `release_id` 不一致会导致设备在不同源之间来回震荡，视为发布事故。

### 7.8 更新强制语义（C11）

```text
update_required = forced
                  OR (min_client_version 不为空 AND 客户端 app_version < min_client_version)
```

`app_version` 为点分数字段，服务端按数值逐段比较（不是字符串比较）。客户端只读 `update_required`，不做版本比较。

`update_required = true` 时第一阶段的客户端行为：提示用户"必须更新"，但**不阻断已配置任务的执行**（脚本可用性优先）；后续版本可改为阻断。

### 7.9 已禁用版本的查询

```text
GET /api/v1/releases/{release_id}
    enabled = 1           → 200
    enabled = 0 或已删除   → 410 + RELEASE_DISABLED
    不存在                → 404 + NOT_FOUND
```

---

## 8. 运行会话与时长规则（重写）

### 8.1 时间表示（C4）

| 层次 | 表示 | 说明 |
|---|---|---|
| 数据库 | `INTEGER` Unix 秒（UTC） | 便于区间算术与索引；不受容器时区影响 |
| 接口 | ISO 8601 字符串，UTC（如 `2026-09-12T12:30:00Z`） | 便于人读与跨语言 |
| 展示 | 北京时间（`Asia/Shanghai`） | 只在前端/页面转换，服务端不存本地时间 |
| 排行榜区间 | 北京时间自然日/周/月 → 换算为 epoch 秒后查询 | 见第 9.2 节 |

服务端**不得**信任客户端提交的任何时间戳；`started_at` 以服务端首次收到开始请求的时间为准。

### 8.2 心跳

```text
HEARTBEAT_INTERVAL_SECONDS        = 180      # 客户端建议间隔（服务端在响应中下发）
HEARTBEAT_MIN_WRITE_INTERVAL_SECONDS = 60    # 服务端写节流：60 秒内的重复心跳只返回不写库
HEARTBEAT_TIMEOUT_SECONDS         = 600      # 10 分钟无心跳即判定掉线
```

心跳请求体：

```json
{
  "install_id": "ER-7F3A-91C2-8D44",
  "client_seq": 42,
  "app_version": "2.0.0.8",
  "release_id": "v2.0.0.8-94503c6d0762"
}
```

- `client_seq`：客户端单调递增序号（可选）。服务端用 `MAX()` 语义处理乱序与重放，**任何情况下 `last_heartbeat_at` 不得回退**；
- 心跳写入使用 `last_heartbeat_at = MAX(last_heartbeat_at, now)`、`heartbeat_count = heartbeat_count + 1`；
- 心跳响应回传 `server_time`（客户端据此校正时钟）与 `next_heartbeat_seconds`。

### 8.3 会话结束规则（C5）

| 场景 | 触发 | `status` | `ended_at` | `end_reason` |
|---|---|---|---|---|
| 正常停止 | `POST /runs/{run_id}/stop` | `stopped` | 服务端收到时刻 | 客户端提交的 reason |
| 被新会话替换 | 同实例发起新 `POST /runs` | `stopped` | 新会话开始时刻 | `replaced_by_new_run` |
| 心跳超时 | scheduler 扫描 | `timeout` | `last_heartbeat_at + 120s`（不超过扫描时刻） | `heartbeat_timeout` |
| 管理员手动关闭 | 后台操作 | `stopped` | 操作时刻 | `server_closed` |
| 数据异常 | 校验失败（见 8.7） | `invalid` | 判定时刻 | 具体原因 |

```text
HEARTBEAT_GRACE_SECONDS = 120
ended_at(timeout) = MIN(scan_time, last_heartbeat_at + HEARTBEAT_GRACE_SECONDS)
duration_seconds  = MAX(0, ended_at - started_at)
```

**为什么需要容差：** 脚本的定时下线路径直接调用 `system.exit()`（`AppGame.timed_offline_detection`），进程被强杀时不会调用停止接口；被游戏掉线重连、设备关机等场景中断时同样如此。**多数会话注定以 `timeout` 结束**，若按 `last_heartbeat_at` 结算，每个会话会系统性少算最多一个心跳间隔。容差必须小于超时阈值。

`end_reason` 枚举：

```text
user_stopped            # 用户在脚本界面停止
task_finished           # 队列执行完毕
script_exit             # 脚本进程正常退出
replaced_by_new_run     # 被同一实例的新会话替换
server_closed           # 管理员手动关闭
heartbeat_timeout       # 心跳超时
invalid_data            # 数据异常被判定无效
unknown                 # 兜底
```

不在枚举内的 reason 返回 400 `INVALID_ARGUMENT`（不做静默兜底，避免客户端写错值却查不出来）。

### 8.4 幂等（C6）

以下请求必须幂等，并且**由数据库约束保证**，不依赖应用层"先查后写"：

| 请求 | 幂等语义 | 落地手段 |
|---|---|---|
| 重复注册同一 `install_id` | 不新建实例，只更新版本信息与 `last_seen_at`；**不覆盖已存在的非空 `display_name`** | `installations.install_id` UNIQUE + UPSERT |
| 重复开始同一 `run_id` | 返回同一会话，`started_at` 不重置、不重复计数 | `runtime_runs.run_id` UNIQUE + 冲突时返回既有记录 |
| 重复发送同一心跳 | 不重复累计时长（时长本就不累计，改为区间计算）；`last_heartbeat_at` 不回退 | `MAX()` 语义 + 60 秒写节流 |
| 重复停止同一 `run_id` | 返回首次结果，`ended_at` / `end_reason` 不被覆盖 | `UPDATE ... WHERE status = 'active'` 影响行数为 0 时返回既有记录 |

严格禁止：重复创建安装实例、创建多个相同会话、已结束会话重新变为运行中。

### 8.5 并发会话（C6）

同一 `install_id` 只能有一个活动会话，用**部分唯一索引**强制：

```sql
CREATE UNIQUE INDEX ux_runtime_runs_active_per_installation
    ON runtime_runs (installation_id)
    WHERE status = 'active';
```

新的开始请求在**单个事务**内完成（SQLite 用 `BEGIN IMMEDIATE`）：

```text
1. 找到该实例的活动会话（若有）
2. 将其关闭：status='stopped', end_reason='replaced_by_new_run',
   ended_at=now, duration_seconds=now-started_at
3. 插入新会话（status='active', started_at=now, last_heartbeat_at=now）
4. 提交；响应中回传被替换的 replaced_run_id
```

若第 3 步因唯一索引冲突失败，重试一次（最多 3 次），仍失败返回 409 `CONFLICT`。

### 8.6 归属校验（C18 的组成部分）

心跳与停止请求中的 `install_id` 必须与 `run_id` 所属实例一致：

```text
run 不存在                        → 404 RUN_NOT_FOUND
run.installation_id != install_id → 403 FORBIDDEN
```

这是**完整性校验**，不是鉴权：它防止跨实例误操作与简单的脚本 bug 污染别人数据，但不能阻止有意伪造（见第 14.3 节）。

### 8.7 异常数据保护

```text
SESSION_MAX_SECONDS = 2592000     # 30 天：超过则判定为异常
1. started_at > now + 300s        → status='invalid', end_reason='invalid_data'
2. duration_seconds > SESSION_MAX_SECONDS → 时长截断到上限并标 invalid
3. ended_at < started_at          → 时长按 0、ended_at 归位到 started_at，并标 invalid
4. 超过 30 天仍处于 active 的会话    → scheduler 关闭；因命中第 2 条，最终标 invalid
```

收到 `invalid` 会话的时长**不计入排行榜**（见 9.1），但保留在库中供排查。

实现上，第 1～3 条统一收敛在 `app/services/session.py::_finalize_run()` 里：它是所有
"结束会话"路径（停止、被替换、超时扫描）的唯一出口，因此不会出现某条路径忘了做保护。
第 4 条的时长必然超过上限，所以按第 2 条截断并标 `invalid`——这比标 `timeout`
更能表达"这条记录本身不可信"，也就不会被算进榜单。

---

## 9. 排行榜计算规则（重写，C14）

### 9.1 统计口径

```text
某 install_id 在区间 [period_start, now] 内的有效时长
  = Σ 每条会话与区间的交集秒数
```

- 计入 `status ∈ {active, stopped, timeout}` 的会话；
- **不计入** `status = 'invalid'` 的会话；
- 活动会话按"截至查询时刻"参与计算（不冻结，实时反映当前挂机时长）；
- **统一现算，不读取 `duration_seconds` 字段**——避免"总榜读存量字段、周期榜现算"导致两套口径对不上；`duration_seconds` 仅用于会话列表展示。
- 被禁用的实例从排行榜剔除，历史数据保留。

### 9.2 区间边界（北京时间）

```text
today : 北京时间当天 00:00:00 → now
week  : 北京时间本周一 00:00:00 → now
month : 北京时间当月 1 日 00:00:00 → now
all   : period_start = 0
```

边界时间按 `Asia/Shanghai` 计算后换算为 epoch 秒。跨日期、跨周、跨月的会话由 9.3 的 clamp 自动切分，**不得把整段时长算到开始日期**。

### 9.3 clamp 算法与 SQL

```text
对每条会话 i：
    有效结束 = MIN(COALESCE(ended_at, now), now)
    有效开始 = MAX(started_at, period_start)
    交集     = MAX(0, 有效结束 - 有效开始)
```

```sql
SELECT
    r.installation_id,
    SUM(
        MAX(0, MIN(COALESCE(r.ended_at, :now), :now) - MAX(r.started_at, :period_start))
    ) AS duration_seconds,
    MAX(COALESCE(r.ended_at, r.last_heartbeat_at)) AS last_active_at
FROM runtime_runs r
WHERE r.status IN ('active', 'stopped', 'timeout')
  AND COALESCE(r.ended_at, :now) > :period_start
  AND r.started_at < :now
GROUP BY r.installation_id
ORDER BY duration_seconds DESC, r.installation_id ASC
LIMIT :limit OFFSET :offset;
```

> **实现说明（M3 已落地）**：代码里用的是"数据库侧先按 `WHERE` 过滤、再按
> `installation_id` 在 Python 里调 `aggregate_seconds()` 求和"，而不是把上面这段
> `SUM/MAX/MIN` 直接下推。两者口径完全一致（过滤条件用的是同一条
> `COALESCE(ended_at, now) > period_start AND started_at < now`），这样做的原因是
> clamp 只有一处实现（`app/services/duration.py` 的纯函数），能被单测直接锁死；
> 上面的 SQL 保留为**语义参考**与后续优化方向——当 `runtime_runs` 到 10 万级、
> 全表总榜变慢时，再把这段 SQL 下推（文档 9.5 的触发信号）。

> 注意：`SUM(...) AS duration_seconds` 是**现算结果**，刻意与表中同名的存量列 `runtime_runs.duration_seconds` 重名——SQLite 在 `ORDER BY` 中优先解析结果列别名，因此排序用的是现算值。**任何情况下都不得改成读取存量列**，否则总榜与周期榜会出现两套口径。

排序与分页（定死，保证结果稳定可复现）：

```text
ORDER BY duration_seconds DESC, install_id ASC
limit  : 默认 100，上限 200（超限返回 400 INVALID_ARGUMENT）
offset : 上限 10000
rank   : 分页内位置（offset + 序号 + 1），不做并列名次
```

### 9.4 响应

```json
{
  "period": "month",
  "period_start": "2026-08-31T16:00:00Z",
  "period_end": "2026-09-12T12:30:00Z",
  "timezone": "Asia/Shanghai",
  "generated_at": "2026-09-12T12:30:00Z",
  "total": 137,
  "limit": 100,
  "offset": 0,
  "items": [
    {
      "rank": 1,
      "install_id": "ER-7F3A-91C2-8D44",
      "display_name": "小明",
      "display_name_fallback": "ER-7F3A-91C2-8D44",
      "duration_seconds": 151200,
      "duration_hours": 42.0,
      "last_active_at": "2026-09-12T12:30:00Z"
    }
  ]
}
```

- `display_name` 为空时，服务端直接返回 `install_id` 作为显示值，客户端不重复实现规则；`display_name_fallback` 保留原始回退值；
- `duration_hours` 保留 2 位小数（展示便利）；
- v1 的 `device_count` 在第一阶段恒为 1，**本版移除**；第二阶段实现多设备合并时再加回。

### 9.5 性能与后续演进

- 依赖索引 `(status, ended_at)`、`(installation_id, started_at)`、`(status, last_heartbeat_at)`；区间查询只扫描区间内会话；
- 单表行数在 10 万级时，全表总榜聚合仍是毫秒~百毫秒级，配合 `limit` 上限足够第一阶段使用；
- 出现以下任一信号时再引入 `runtime_daily_stats` 日汇总表：总榜查询 > 500ms、`runtime_runs` > 500 万行、或需要历史榜单快照。

---

## 10. API 契约

### 10.1 通用约定

```text
前缀：/api/v1/（健康检查与 /dist 兼容路径除外）
Content-Type：application/json; charset=utf-8
时间：ISO 8601 UTC，秒级精度，形如 2026-09-12T12:30:00Z
请求体上限：客户端接口 16 KiB（心跳 2 KiB / 注册 4 KiB / 资料 1 KiB），超限 413
追踪：服务端接受并回写 X-Request-ID（缺失时自行生成），错误响应体中带 request_id
压缩：对 > 1 KiB 的响应启用 gzip
分页：列表响应统一为 {total, limit, offset, items}；客户端接口 limit 默认 100 / 上限 200，
      管理后台接口 limit 默认 20 / 上限 200；offset 上限统一 10000；超限返回 400
```

### 10.2 客户端接口总表

| 方法 | 路径 | 用途 | 鉴权 | 幂等 |
|---|---|---|---|---|
| GET | `/dist/latest.json` | 零改动兼容清单 | 无 | 是 |
| GET | `/dist/releases/{filename}` | 302 → OSS | 无 | 是 |
| GET | `/api/v1/releases/latest` | 规范清单接口 | 无 | 是 |
| GET | `/api/v1/releases/{release_id}` | 查询指定版本 | 无 | 是 |
| POST | `/api/v1/installations/register` | 注册/刷新实例 | 无 | 是 |
| GET | `/api/v1/installations/{install_id}` | 查询实例档案 | 无 | 是 |
| PATCH | `/api/v1/installations/{install_id}/profile` | 修改显示名 | 无（限流） | 是 |
| POST | `/api/v1/runs` | 开始会话 | 无 | 是 |
| POST | `/api/v1/runs/{run_id}/heartbeat` | 心跳 | 无 | 是 |
| POST | `/api/v1/runs/{run_id}/stop` | 停止会话 | 无 | 是 |
| GET | `/api/v1/rankings/runtime-duration` | 排行榜 | 无 | 是 |
| GET | `/healthz` | 健康检查 | 无 | 是 |

> "鉴权：无"是**已确认的决策**（C18）。所有写接口依赖限流 + 格式校验 + 归属校验，残余风险见第 14.3 节。

### 10.3 关键接口细节

**POST /api/v1/installations/register**

```json
{ "install_id": "ER-7F3A-91C2-8D44", "display_name": "", "app_version": "2.0.0.8",
  "release_id": "v2.0.0.8-94503c6d0762", "update_channel": "stable" }
```

响应 200：

```json
{ "install_id": "ER-7F3A-91C2-8D44", "display_name": "", "display_name_effective": "ER-7F3A-91C2-8D44",
  "status": "active", "update_channel": "stable",
  "first_seen_at": "2026-09-01T00:00:00Z", "last_seen_at": "2026-09-12T12:30:00Z",
  "server_time": "2026-09-12T12:30:00Z", "heartbeat_interval_seconds": 180 }
```

- `display_name` 为空**不覆盖**已有的非空名字；非空时按 6.3 校验并更新；
- `update_channel` 仅在首次注册时生效（后续由后台管理或专用接口调整）；
- `status = disabled` 的实例注册返回 403 `INSTALLATION_DISABLED`。

**POST /api/v1/runs**

```json
{ "install_id": "ER-7F3A-91C2-8D44", "run_id": "ER-7F3A-91C2-8D44-20260912T200000-a3f9c1",
  "app_version": "2.0.0.8", "release_id": "v2.0.0.8-94503c6d0762" }
```

响应 201（新建）/ 200（幂等重放）：

```json
{ "run_id": "ER-...-a3f9c1", "status": "active",
  "started_at": "2026-09-12T12:00:00Z", "server_time": "2026-09-12T12:00:00Z",
  "heartbeat_interval_seconds": 180, "replaced_run_id": null }
```

**POST /api/v1/runs/{run_id}/heartbeat** → 200

```json
{ "run_id": "ER-...-a3f9c1", "status": "active", "server_time": "2026-09-12T12:03:00Z",
  "last_heartbeat_at": "2026-09-12T12:03:00Z", "duration_seconds_so_far": 180,
  "next_heartbeat_seconds": 180 }
```

会话已结束 → 409 `RUN_ALREADY_STOPPED`；客户端收到该错误时，应生成**新的 `run_id`** 重新开始会话（已结束会话不得复活）。

**POST /api/v1/runs/{run_id}/stop** → 200

```json
{ "run_id": "ER-...-a3f9c1", "status": "stopped", "started_at": "2026-09-12T12:00:00Z",
  "ended_at": "2026-09-12T15:30:00Z", "duration_seconds": 12600, "end_reason": "user_stopped" }
```

**GET /api/v1/rankings/runtime-duration** → 见 9.4；`period ∈ {all, month, week, today}`。

### 10.4 限流与请求体大小（C18 的缓解措施）

| 接口 | 键 | 限额 | 超限响应 |
|---|---|---|---|
| `register` | 来源 IP | 5 / 分钟，60 / 天 | 429 `RATE_LIMITED` |
| `register` | `install_id` | 10 / 天 | 429 |
| `runs` | IP + `install_id` | 10 / 分钟 | 429 |
| `heartbeat` | IP + `install_id` | 20 / 分钟 | 429（60 秒内重复心跳正常返回但不写库） |
| `stop` | IP + `install_id` | 10 / 分钟 | 429 |
| `profile` | `install_id` | 3 / 小时，20 / 天 | 429 |
| `releases/latest`、`/dist/*` | IP | 30 / 分钟 | 429 |
| `rankings` | IP | 60 / 分钟 | 429 |
| 管理后台登录 | IP + 账号 | 5 次失败 / 15 分钟，锁定 15 分钟 | 429 / 423 |
| 管理后台其他接口 | 会话 | 300 / 分钟 | 429 |

- 单进程内存计数器即可（uvicorn 固定 1 worker，见 13.1）；重启后计数清零可接受；
- 超出请求体上限返回 413 `INVALID_ARGUMENT`；
- 限流只提高滥用的成本，**不阻止定向伪造**（见 14.3）。

---

## 11. 数据库设计

### 11.1 通用约定（C4）

- 主键统一 `id INTEGER PRIMARY KEY AUTOINCREMENT`；
- 时间列统一 `INTEGER`（Unix 秒，UTC），命名 `*_at`；可为空的结束时间用 `NULL`；
- 布尔用 `INTEGER` 0/1；
- 字符串一律 UTF-8，`install_id` / `run_id` / `release_id` 带唯一索引；
- SQLite 连接参数：`journal_mode=WAL`、`busy_timeout=5000`、`synchronous=NORMAL`、`foreign_keys=ON`；
- 所有"读-改-写"会话状态的操作使用 `BEGIN IMMEDIATE` 事务。

### 11.2 表结构

**admins**

```text
admins
├── id
├── username            UNIQUE
├── password_hash                        # Argon2id
├── is_active
├── failed_login_count
├── locked_until        NULL
├── must_change_password                 # 引导管理员改密前为 1（M4 新增）
├── last_login_at       NULL
├── last_login_ip       NULL
├── created_at
└── updated_at
```

**runtime_settings**（M4 新增）

```text
runtime_settings
├── key                 PRIMARY KEY
├── value               NOT NULL
├── updated_at
└── updated_by_admin_id  → admins.id（ON DELETE SET NULL）
```

只存**允许运行时修改**的少量开关（当前是排行榜掩码与备份保留策略），
读取顺序：数据库覆盖值 > 环境变量默认值。连接串、密钥、心跳阈值这类影响进程行为的
配置仍然只走环境变量，避免出现"改了值但要重启才生效"的半吊子状态。

**installations**

```text
installations
├── id
├── install_id                UNIQUE
├── display_name              NOT NULL DEFAULT ''
├── status                    active / disabled
├── update_channel            stable / beta      # C10
├── current_app_version       NOT NULL DEFAULT ''
├── current_release_id        NOT NULL DEFAULT ''  # C8
├── first_seen_at
├── last_seen_at
├── first_ip                  NULL              # 滥用控制
├── last_ip                   NULL
├── created_at
└── updated_at
```

**runtime_releases**

```text
runtime_releases
├── id
├── release_id                UNIQUE              # 由构建工具生成，禁止自造
├── app_version
├── channel                   stable / beta
├── package_path                                 # dist/releases/xxx.zip
├── package_base_url                             # 如 https://as-erchong.oss-...
├── package_sha256
├── package_size                                 # 必须 ≤ 20 MiB
├── mirror_urls               JSON 数组，可空
├── release_notes             NOT NULL DEFAULT ''
├── min_client_version        NULL
├── forced                    0 / 1
├── enabled                   0 / 1
├── verified_status           unverified / ok / failed    # C13
├── verified_at               NULL
├── verify_error              NULL
├── published_at
├── created_at
└── updated_at
```

**release_channels**（C9）

```text
release_channels
├── channel                   PRIMARY KEY（stable / beta）
├── release_id                NOT NULL
├── updated_at
└── updated_by_admin_id
```

**runtime_runs**

```text
runtime_runs
├── id
├── run_id                    UNIQUE
├── installation_id           → installations.id
├── release_id                NOT NULL DEFAULT ''   # C8
├── app_version               NOT NULL DEFAULT ''
├── status                    active / stopped / timeout / invalid
├── started_at
├── last_heartbeat_at
├── ended_at                  NULL
├── duration_seconds          NULL
├── end_reason                NULL
├── heartbeat_count           NOT NULL DEFAULT 0
├── client_seq                NOT NULL DEFAULT 0
├── client_ip                 NULL
├── created_at
└── updated_at

约束（必须在迁移中建出，不能只靠应用层保证）：
  CHECK (status IN ('active','stopped','timeout','invalid'))
  CHECK (status = 'active' OR ended_at IS NOT NULL)   -- 已结束的会话必须有结束时间
```

> 第二条 `CHECK` 是排行榜正确性的一部分：第 9.3 节的 clamp 用 `COALESCE(ended_at, now)` 把"没有结束时间的会话"视为仍在运行。如果库里出现 `status='stopped'` 却没有 `ended_at` 的脏数据，它会被当成**至今仍在挂机**而无限增长榜单时长。应用层的"先查后写"挡不住这种脏数据，用 `CHECK` 一次挡住。（已在本机 SQLite 3.53 实测：两条约束均按预期拒绝非法写入。）

**admin_operation_logs**

```text
admin_operation_logs
├── id
├── admin_id
├── action
├── target_type
├── target_id
├── detail_json
├── ip_address
└── created_at
```

至少记录：发布版本、校验发布、启用/禁用版本、切换渠道指针、回滚、修改用户名、隐藏排行榜名称、禁用/启用安装实例、手动结束会话、修改后台设置、管理员登录成功与失败。

**runtime_daily_stats（可选，第二阶段）**

```text
runtime_daily_stats
├── id
├── install_id
├── stat_date
├── duration_seconds
├── created_at
└── updated_at
```

### 11.3 索引清单（必须创建）

```sql
CREATE UNIQUE INDEX ux_installations_install_id      ON installations (install_id);
CREATE        INDEX ix_installations_last_seen       ON installations (last_seen_at);
CREATE        INDEX ix_installations_channel_status  ON installations (update_channel, status);

CREATE UNIQUE INDEX ux_runtime_runs_run_id           ON runtime_runs (run_id);
CREATE UNIQUE INDEX ux_runtime_runs_active_per_inst  ON runtime_runs (installation_id) WHERE status = 'active';
CREATE        INDEX ix_runtime_runs_inst_started     ON runtime_runs (installation_id, started_at);
CREATE        INDEX ix_runtime_runs_status_heartbeat ON runtime_runs (status, last_heartbeat_at);
CREATE        INDEX ix_runtime_runs_status_ended     ON runtime_runs (status, ended_at);
CREATE        INDEX ix_runtime_runs_release          ON runtime_runs (release_id);

CREATE UNIQUE INDEX ux_runtime_releases_release_id   ON runtime_releases (release_id);
CREATE        INDEX ix_runtime_releases_channel      ON runtime_releases (channel, enabled, published_at);

CREATE UNIQUE INDEX ux_admins_username               ON admins (username);
CREATE        INDEX ix_admin_operation_logs_created_at ON admin_operation_logs (created_at);
CREATE        INDEX ix_admin_operation_logs_admin    ON admin_operation_logs (admin_id, created_at);
```

合计 14 个索引（`ux_runtime_runs_active_per_inst` 是部分唯一索引，见 8.5）。
`tests/test_schema.py` 会比对迁移建出的索引集合与模型声明是否完全一致，防止漏建。

### 11.4 数据迁移

- 使用 Alembic，且 **SQLite 必须开启 batch 模式**（`render_as_batch=True`）——SQLite 不支持大部分 `ALTER TABLE`；
- 迁移在容器启动时执行（`alembic upgrade head`），只由 backend 容器执行，scheduler 等待其完成（见 13.1）；
- 每次模型变更必须带迁移脚本；生产库版本落后于代码视为部署失败，健康检查应能反映。
- **`alembic.ini` 必须保持纯 ASCII**（已实测）：Alembic 用 configparser 读取它，而 configparser 按操作系统区域编码打开文件（中文 Windows 是 GBK）。文件里只要有一个中文字符，`alembic upgrade head` 就会在读配置阶段抛 `UnicodeDecodeError`，且报错信息**不显示是哪个文件**，排查成本很高。中文说明写在文档与代码里，不要写进这个 ini；`tests/test_schema.py` 有专门的回归用例。
- **迁移前必须先确保 SQLite 文件所在目录存在**（已实测）：SQLite 不会自动创建目录，缺目录时只报 `unable to open database file`，看不出真实原因。`app/core/storage.py` 提供 `ensure_sqlite_directory()`，应用启动与 `alembic/env.py` 都会调用。

### 11.5 备份与恢复（C15）

```text
备份：scheduler 容器每日 03:30（北京时间）执行
      sqlite3 /data/erchong.db ".backup '/data/backups/erchong-YYYYMMDD.db'"
      （不要直接 cp 正在使用的数据库文件：WAL 模式下副本可能不一致）
      应用内改用 SQL 备份时注意：`VACUUM INTO` **必须在事务外执行**（先 commit，
      或使用 autocommit 连接），否则报 `cannot VACUUM from within a transaction`（已实测）
保留：日备 14 份 + 月备 12 份，超出自动清理
恢复：docker compose stop backend scheduler
      cp <备份> /data/erchong.db && rm -f /data/erchong.db-wal /data/erchong.db-shm
      docker compose up -d
      # 未通过恢复演练的备份不算备份：上线后必须实际演练一次并记录
```

可选：把备份文件同步到 OSS（使用后台服务器上的 AccessKey，见 14.4）。

---

## 12. 管理员后台

### 12.1 实现方式（C16 / C21）

**服务端渲染**：Jinja2 模板 + htmx 局部刷新 + Bootstrap 5.3 CSS。第一阶段不引入 SPA 框架。

```text
模板    base.html（导航 / 安全头 / CSRF 隐藏域）+ 每页一个模板 + 表格片段模板
交互    htmx 2.x（单文件，无构建）：换页、过滤、局部刷新用 hx-get 替换结果片段
样式    Bootstrap 5.3 CSS（只引 CSS，不引 Bootstrap 自带的 JS）
分页    一律用 GET query 参数（?page=2&status=active&install_id=...）
        —— 可刷新、可分享、可回退；htmx 只替换结果区域，不接管整页状态
静态    全部 vendor 进 app/admin/static/vendor/，确切版本与许可证记入同目录 README.md
图表    第一阶段只用数字卡片（纯 CSS）；需要图表时再引入单文件库或服务端渲染 SVG
部署    与 API 同一进程、同一端口，由反向代理转发；/admin/* 与 /api/* 同源，
        因此不需要额外 CORS，Cookie 与 CSRF 配置也保持最简单
```

**硬约束：**

1. **不走 CDN。** 第 12.4 节的 CSP 是 `script-src 'self'`，引 CDN 就必须放宽；且国内网络下 CDN 不稳定，vendor 入库还能离线开发与审计版本。
2. **不引入 Alpine.js。** 其默认构建依赖 `new Function` 求值，严格 CSP 下必须加 `unsafe-eval` 或改用 `@alpinejs/csp` 构建。本阶段需要的交互（下拉开合、确认弹窗）用少量原生 JS 足够，不值得为它放宽 CSP。
3. **不在 ECS 上运行 Node 构建。** 当前方案本身没有 Node 依赖，这条约束自动满足；将来若改 SPA，必须是本地构建后上传的静态产物。

**分层（关键）：页面路由与 JSON 接口都只是薄壳。**

```text
app/services/    业务逻辑与校验（纯函数 + service 类，可单测）
app/api/v1/      JSON 接口薄壳：解析请求 → 调 service → 序列化
app/admin/       HTML 路由薄壳：解析表单 → 调 service → 渲染模板
```

CSRF、限流、审计日志、`display_name` 校验、会话状态机这些规则**只能有一份实现**。否则页面与 API 会各自演化，迟早出现"页面改名成功、API 改名失败"这类只在一边复现的问题。管理后台的 JSON API 保持独立可用（`docs/api/openapi-admin.yaml` 已冻结），因此将来若改做 SPA，后端不需要改动。

### 12.2 认证与会话

```text
密码哈希：Argon2id（内存 64 MiB / 迭代 3 / 并行 1），禁止明文与可逆加密
会话：签名 Cookie（itsdangerous），HttpOnly + Secure + SameSite=Lax + Path=/
      空闲过期 12 小时，绝对过期 7 天
CSRF：双重提交 Token（Cookie + 表单隐藏字段/请求头 X-CSRF-Token），所有写操作校验
登录：失败 5 次 / 15 分钟锁定 15 分钟，失败与成功均记入操作日志（含 IP）
首个管理员：由环境变量 ADMIN_BOOTSTRAP_USERNAME / ADMIN_BOOTSTRAP_PASSWORD 在首次启动时创建，
           强制首次登录改密；引导变量在改密后即失效（数据库标记 bootstrap_done）
退出：删除服务端会话记录 + 清 Cookie
生产环境：必须 HTTPS（由 Caddy 自动签发），HTTP 强制跳转
```

### 12.3 页面清单

| 页面 | 内容 |
|---|---|
| 登录页 | 账号、密码、失败提示、限流提示 |
| 首页概览 | 实例总数、当前在线实例数、活动会话数、今日/本周/本月运行时长、当前正式版本、最近一次发布、校验失败告警 |
| 运行时版本管理 | 创建发布记录、编辑说明、触发校验、查看校验结果、stable/beta 渠道、启用/禁用、强制更新开关、最低客户端版本、切换渠道指针（回滚）、查看各版本使用实例数 |
| 安装实例管理 | `install_id`、显示名、首次/最后活动时间、当前版本与 `release_id`、当前活动会话、累计运行时长、状态、首次 IP；操作：改显示名、隐藏排行榜名称、禁用/启用、查看会话、手动结束会话 |
| 运行会话列表 | `run_id`、`install_id`、开始时间、最后心跳、结束时间、时长、结束原因、`release_id`；可按状态/实例/时间筛选 |
| 排行榜 | 总榜、月榜、周榜、今日榜；排名、显示名（空时显示回退值）、时长、最后活动时间 |
| 操作日志 | 管理员、动作、对象、时间、IP、详情 JSON |
| 系统设置 | 排行榜掩码开关、心跳阈值展示、备份状态 |

### 12.4 XSS / CSP（C16）

`display_name` 是**用户可控内容**，会渲染进管理页面：

- Jinja2 全局开启 `autoescape`，任何用户输入一律用 `{{ }}` 输出，禁止 `|safe`；
- 输入侧按 6.3 白名单校验，作为第二道防线；
- 响应头：`Content-Security-Policy: default-src 'self'; script-src 'self'; object-src 'none'; base-uri 'none'; frame-ancestors 'none'`；htmx 与 Bootstrap CSS 必须以本地 vendor 文件引入（见 12.1），**禁止 CDN、禁止 `unsafe-inline` / `unsafe-eval`**；htmx 的 CSRF 头通过 `<body hx-headers>` 注入，避免内联脚本；
- 其他安全头：`X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer`、`X-Frame-Options: DENY`。

---

## 13. Docker 部署

### 13.1 服务拓扑（C15）

```text
ECS (2 核 2G)
└── Docker Compose
    ├── backend     uvicorn（固定 1 worker）+ 管理页面 + API
    ├── scheduler   同镜像不同命令：超时扫描、发布校验、备份
    ├── reverse-proxy  Caddy（自动 HTTPS）
    └── volume: backend_data（SQLite + 备份）
```

**关键约束：**

- **backend 固定 1 worker。** 多 worker 会让内存限流计数、后台定时任务重复执行；2 核 2G 也不需要多 worker；
- **超时扫描只能有一份**，因此放在独立 scheduler 进程（同镜像，`command: python -m app.scheduler`），而不是 FastAPI 的启动钩子；
- scheduler 启动时等待数据库迁移完成（重试连接，最多 60 秒）；
- `restart: unless-stopped`；健康检查失败不自动重建数据库卷。

### 13.2 环境变量（`.env.example`）

```text
APP_ENV=production
TZ=Asia/Shanghai                     # 仅影响日志显示；数据一律 UTC
DATABASE_URL=sqlite:////data/erchong.db
ADMIN_BOOTSTRAP_USERNAME=admin
ADMIN_BOOTSTRAP_PASSWORD=change-me
SESSION_SECRET=<32 字节随机>
PACKAGE_BASE_URL=https://as-erchong.oss-cn-beijing.aliyuncs.com/
PACKAGE_MIRROR_URLS=https://raw.githubusercontent.com/qingushan/as-erchong/runtime/
RANKING_MASK_INSTALL_ID=false         # true 时公开排行榜以短码代替完整 install_id
LOG_LEVEL=info
BACKUP_ENABLED=true
BACKUP_KEEP_DAILY=14
BACKUP_KEEP_MONTHLY=12
```

### 13.3 日志

```text
Docker 日志轮转：max-size 10m / max-file 3（backend、scheduler、caddy 全部配置）
应用日志：JSON 行格式，含 request_id、路径、状态码、耗时、install_id（若有）
禁止把请求体整体写入日志（可能含 display_name 等用户内容）
```

### 13.4 健康检查

```text
GET /healthz  → 200 {"status":"ok","db":"ok","version":"...","release_id":"...","server_time":"...Z"}
                503 {"status":"degraded","db":"error",...}
```

检查项：进程存活、数据库可执行 `SELECT 1`、迁移版本与代码期望一致。用于 Compose healthcheck 与外部探活。

### 13.5 定时任务（scheduler）

| 任务 | 频率 | 说明 |
|---|---|---|
| 会话超时扫描 | 每分钟 | 关闭超过 `HEARTBEAT_TIMEOUT_SECONDS` 的活动会话，按 8.3 计算 `ended_at` |
| 发布校验重试 | 每 5 分钟 | 处理 `verified_status = unverified` 的记录 |
| 每日备份 | 每日 03:30 | 见 11.5 |
| 数据保留清理 | 每日 04:00 | 见 15.2 |

无需 Celery / Redis / 消息队列。

---

## 14. 安全要求与已接受风险

### 14.1 管理员侧

- Argon2id 哈希、不保存明文；
- 管理接口必须鉴权（会话 Cookie + CSRF）；
- 登录失败限流与账号锁定；
- 会话过期；
- 生产必须 HTTPS；
- 发布、回滚、禁用、改配置全部写审计日志。

### 14.2 客户端接口侧

客户端代码可见，因此**不得把客户端请求视为可信**。已实施：

- HTTPS；
- 请求频率限制与请求体大小限制（第 10.4 节）；
- 参数格式与长度校验（`install_id`、`run_id`、`display_name`、`reason` 枚举）；
- 归属校验（8.6）；
- 幂等与重复请求处理；
- **禁止客户端提交最终累计时长**。

客户端内置的固定密钥不能被当作真正的安全凭证——这也是本阶段不引入共享密钥的原因之一（见 14.3）。

### 14.3 已接受的风险（C18，明确记录）

**决策：** 第一阶段客户端写接口不做身份鉴权（无 `install_secret`、无 HMAC 签名），只做限流 + 格式校验 + 归属校验。理由是降低脚本端接入成本，且客户端内置密钥本身也挡不住有意攻击。

**残余风险（必须知情）：**

| 风险 | 影响 | 现有缓解 |
|---|---|---|
| 知道 `install_id` 即可修改他人显示名 | 恶作剧改名 | 3 次/小时限流、审计日志、后台可改回、可禁用实例 |
| 知道 `install_id` 即可伪造会话刷时长 | 排行榜失真 | 限流、格式校验、会话时长上限、异常会话可标 `invalid` 并被后台剔除 |
| 公开排行榜返回完整 `install_id` | 降低伪造门槛 | 提供 `RANKING_MASK_INSTALL_ID` 配置（默认 `false`，开启后公开接口以短码代替完整 ID，不影响契约字段） |
| 批量注册 `install_id` 灌榜 | 排行榜被噪声占用 | 按 IP 的注册限流（5/分钟、60/天）、后台可批量禁用 |

**不承诺"防刷"。** 验收标准（21.1）只验证限流与校验生效，不验证"无法伪造"。

**后续兼容升级路径（第二阶段，接口向后兼容）：** 新增加密的请求头 `X-Install-Secret`（注册时一次性下发、服务端存哈希）。老客户端不带该头时按当前策略处理；后台可配置"对某渠道强制要求"。届时排行榜可信度可提升，且不需要破坏已有契约。

### 14.4 密钥管理

以下内容只能放在 ECS 后台（`.env` 或 Docker secrets，`.env` 不入库）：

```text
OSS AccessKey / SecretKey
数据库密码（当前 SQLite 无密码，预留）
管理员初始密码、SESSION_SECRET
构建工具 --register 使用的管理 token
```

**禁止**写入：AScript 脚本、`remote_loader.py`、运行时 ZIP、GitHub 仓库、Docker 镜像。

GitHub 仓库必须公开（作为匿名备用下载源），因此更要注意：仓库内不得出现任何凭据；`main` 分支开启 2FA 保护。

### 14.5 数据采集边界

第一阶段只采集：

```text
随机 install_id、用户自定义显示名、脚本版本与 release_id、
更新渠道、会话开始时间、最后心跳时间、会话结束时间、运行时长、来源 IP（限流与滥用排查）
```

不采集：手机号、游戏账号、真实姓名、IMEI、Android 广告 ID、云手机登录密码、游戏截图、完整任务配置、设备型号与系统信息。

---

## 15. 数据保留与容量（C17）

### 15.1 容量估算

```text
心跳写入：每设备 1 条 / 3 分钟
  100 台设备  ≈ 0.55 写/秒
  1000 台设备 ≈ 3.3 写/秒
  5000 台设备 ≈ 16.7 写/秒
```

配合 60 秒写节流，单写者 SQLite（WAL）在 2 核 ECS 上足以支撑数千台设备的写入；读多写少的管理页面与排行榜无压力。

**退出 SQLite 的信号**（出现任一即考虑 MySQL/PostgreSQL）：写延迟 P99 > 200ms、排行榜查询 > 1s、设备数 > 1 万、或需要多实例水平扩展。

### 15.2 保留策略

```text
runtime_runs      ：保留 12 个月明细；更早的数据按月聚合进 runtime_daily_stats 后删除明细
admin_operation_logs：保留 24 个月
限流计数          ：内存，重启即清
备份              ：日备 14 份 + 月备 12 份
```

清理任务由 scheduler 在每日 04:00 执行，并在操作日志中记录清理条数。

---

## 16. 两个项目的职责边界

### AScript 脚本项目

负责：

- 生成和持久化 `install_id`（`KeyValue` 新键，见 6.1）；
- 生成每次运行的 `run_id`（全局唯一格式，见 6.2）；
- 提供用户名设置界面；
- 调用注册、开始会话、心跳、停止接口；
- 清单来源切换（后台优先、OSS/GitHub 兜底）；
- 网络失败时保证脚本主体仍可运行；
- 保留本地必要状态。

不负责：计算最终运行时长、修改排行榜、保存后台密钥、执行管理员操作、上传 OSS AccessKey、生成 `release_id`。

### 后台项目

负责：

- 管理管理员与会话；
- 保存安装实例、显示名与渠道；
- 保存运行会话；
- 统一计算运行时长与排行榜；
- 管理版本元数据、渠道指针与 OSS 地址；
- **发布入库的完整性校验**（size + sha256）；
- 处理会话超时与异常数据；
- 记录管理员操作；
- 提供 Docker 部署与备份。

---

## 17. 接口变更流程

两个项目可以分别使用 Git 仓库，但接口契约必须有唯一来源：

```text
本文档          ：语义、规则、边界（人工可读，唯一权威）
docs/api/*.yaml ：OpenAPI 快照（机器可读，与本文档同步修订）
后台项目        ：按快照实现，并保留快照的历史版本
脚本项目        ：保留客户端所需的接口简化说明
```

变更流程（必须按序）：

1. 先更新本文档与 OpenAPI 快照；
2. 增加接口版本或保持向后兼容（**禁止直接修改已有字段含义**）；
3. 后台先同时支持新旧字段；
4. 脚本端再切换调用；
5. 在真实设备验证；
6. 确认没有旧版本客户端后再删除旧接口。

快照纪律：任何接口改动必须在同一次提交中同时更新 `docs/api/`，并在 PR 描述中写明受影响字段。契约必须明确：路径、方法、请求字段、响应字段、类型、是否必填、时间格式、时间单位、时区、错误码、幂等规则、限流。

---

## 18. 错误响应格式与错误码

```json
{
  "error": {
    "code": "INVALID_ARGUMENT",
    "message": "install_id 格式无效",
    "request_id": "req-9f3c1a7b"
  }
}
```

| 错误码 | HTTP | 触发场景 |
|---|---|---|
| `INVALID_ARGUMENT` | 400 / 413 | 格式、枚举、超限（含请求体过大） |
| `UNAUTHORIZED` | 401 | 管理员未登录 / 会话过期 |
| `FORBIDDEN` | 403 | 归属不符、CSRF 失败、实例被禁用（`INSTALLATION_DISABLED` 见下） |
| `NOT_FOUND` | 404 | 资源不存在、无可用发布记录 |
| `CONFLICT` | 409 | 并发冲突、`RUN_ALREADY_STOPPED` |
| `RATE_LIMITED` | 429 | 触发限流 / 登录失败过多 |
| `RELEASE_DISABLED` | 410 | 查询已禁用的发布版本 |
| `INSTALLATION_DISABLED` | 403 | 实例已禁用仍调用写接口 |
| `RUN_NOT_FOUND` | 404 | `run_id` 不存在 |
| `RUN_ALREADY_STOPPED` | 409 | 对已结束会话发心跳 |
| `ACCOUNT_LOCKED` | 423 | 管理员账号锁定 |
| `INTERNAL_ERROR` | 500 | 未预期错误（不返回堆栈） |

客户端不应因为后台暂时不可用而直接停止脚本主体。

---

## 19. 客户端网络失败策略

后台属于辅助能力，不能影响脚本核心任务执行。

```text
后台请求成功 → 正常记录状态
后台请求失败 → 打印简短日志、本地有限重试、不阻塞主要任务、不无限等待
```

超时与重试（C20）：

```text
启动路径（清单）：连接 2s / 读取 3s，不重试，失败立即回退 OSS
运行期（注册/会话/心跳）：连接 5s / 读取 10s，重试 1 次；心跳失败按 30s → 2min → 10min 退避重试
```

脚本侧实现约束（M6 阶段执行，此处先约定）：

1. 客户端模块必须是**独立线程**，与任务线程解耦——看门狗会用 `ctypes.PyThreadState_SetAsyncExc` 强杀任务线程（`AppGame.py`），心跳不能搭在任务线程里；
2. 线程内所有异常必须吞掉并只打印日志，绝不允许冒泡到 `AppGame.run()`；
3. **停止接口是尽力而为**：定时下线路径直接 `system.exit()`，进程被强杀时不会有 stop 请求，此时依赖 8.3 的超时容差结算；
4. 单例 + 有界队列，避免网络异常时堆积线程；
5. 不得因为排行榜或心跳失败导致脚本退出。

---

## 20. 开发顺序（C3）

v1 的顺序把"最不确定的部分"放在最后（统计在第 5 步、UI 在第 3 步）。调整为先冻契约、再打通最小闭环：

| 阶段 | 内容 | 出口条件 |
|---|---|---|
| **M0** 契约冻结（本版） | 修订本文档 + 产出 `docs/api/` OpenAPI 快照 | 本文档与快照通过评审；P0 项全部落定 |
| **M1** 项目骨架 | 仓库结构、FastAPI 应用、配置、错误格式、`/healthz`、Dockerfile、compose（backend + scheduler + caddy）、`.env.example`、Alembic 初始化、SQLite 参数 | `docker compose up -d` 可启动，`/healthz` 返回 200 |
| **M2** 会话核心（风险集中） | 模型与迁移、`installations` / `runs` 接口、幂等与并发、超时扫描、异常数据保护；`tests/` 覆盖跨日/跨周/乱序/重复/并发 | 假客户端可完成"注册→开始→心跳→停止"；契约测试全绿 |
| **M3** 发布与排行榜 | `/releases/latest`（含 `/dist/latest.json` 兼容路径与 302）、发布入库自校验、渠道指针、排行榜 clamp | 清单响应能被现网 `_validate_manifest` 通过；榜单数字与手算一致 |
| **M4** 管理后台 | 登录、概览、版本管理、实例管理、会话列表、排行榜、操作日志、设置 | 管理员可完成发布→校验→启用→回滚全流程 |
| **M5** 运维与安全收尾 | HTTPS/CSP/安全头、限流、审计补全、备份 + **恢复演练**、日志轮转、数据保留清理、端到端冒烟脚本 | 第 21.1、21.3 节验收全通过 |
| **M6** 脚本接入（独立立项） | `install_id`/`run_id` 生成与持久化、客户端模块与心跳线程、用户名界面、清单来源切换 | 第 21.2 节验收 |

> **实施进度**：M0～M4 已完成，逐项记录见 [附录 D](#附录-d实施记录m1)、[附录 E](#附录-e实施记录m2-会话核心)、[附录 F](#附录-f实施记录m3-发布与排行榜)、[附录 G](#附录-g实施记录m4-管理后台)。
> M3 已用**现网客户端的真实代码**验证清单兼容性；M4 的管理端接口与冻结快照逐字段比对通过，
> 页面在本机 Docker 全链路走查通过（登录 → 强制改密 → 各页面 → CSRF 拦截）。
> ECS 上的真实部署与备份恢复演练仍待 M5。

关键调整理由：会话与时长算法一旦错了，管理 UI 做得再好都要推翻；UI 是纯体力活，可以后置。

建议的后台仓库结构（与脚本项目分离维护）：

```text
AS-二重螺旋后台/
├── 后台开发技术文档.md            # 本契约
├── docs/
│   ├── api/openapi-v1.yaml        # 客户端接口快照
│   ├── api/openapi-admin.yaml     # 管理后台接口快照
│   └── archive/                   # 历史版本文档
├── app/
│   ├── main.py                    # FastAPI 应用装配
│   ├── api/v1/                    # releases / installations / runs / rankings / compat(/dist)
│   ├── admin/                     # 管理路由薄壳 + Jinja2 模板 + static/vendor（htmx/Bootstrap）
│   ├── core/                      # 配置、会话、限流、错误、request_id、安全头
│   ├── models/                    # SQLModel 模型
│   ├── services/                  # 会话、时长与区间计算、排行榜、发布校验（纯函数，易测）
│   └── scheduler/                 # 独立进程入口
├── alembic/
├── tests/                         # 契约测试 + 假客户端 + 时间冻结测试
├── scripts/fake_client.py         # 端到端冒烟用假客户端
├── Dockerfile
├── docker-compose.yml
├── Caddyfile
└── .env.example
```

`app/services/` 中的时长与区间计算必须是**纯函数**（输入会话列表 + 区间 + now，输出秒数），这是排行榜可被单测锁定、避免"数字对不上"扯皮的关键。

`app/api/v1/` 与 `app/admin/` 都只是薄壳，两者共用 `app/services/`；业务规则不允许在两个入口各写一份（见 12.1）。

---

## 21. 验收标准（C3：按项目拆分）

### 21.1 后台验收

**身份与资料**

- [ ] 重复注册同一 `install_id` 不创建重复实例；
- [ ] 注册时 `display_name` 为空不会覆盖已有非空名字；
- [ ] `display_name` 校验：长度、字符集、保留名、去空格；非法值返回 400；
- [ ] 改名频率超限返回 429；
- [ ] 用户名为空时接口返回 `install_id` 作为显示值；
- [ ] `install_id` 格式非法返回 400，且不会写入数据库。

**会话**

- [ ] 重复开始同一 `run_id` 只存在一条会话，`started_at` 不重置；
- [ ] 同一实例发起新会话时，旧会话被关闭且 `end_reason = replaced_by_new_run`；
- [ ] 部分唯一索引生效：并发开始请求不会产生两个活动会话；
- [ ] 重复心跳不重复累计时长，`last_heartbeat_at` 不回退；
- [ ] 60 秒内的重复心跳正常返回但不写库；
- [ ] 重复停止返回首次结果，`ended_at` 不被覆盖；
- [ ] 对已结束会话发心跳返回 409 `RUN_ALREADY_STOPPED`，且会话不会复活；
- [ ] 超过 10 分钟无心跳的会话被自动关闭，`ended_at = last_heartbeat_at + 120s`，`status = timeout`；
- [ ] 心跳/停止时 `install_id` 与会话归属不符返回 403；
- [ ] 时长全部由服务端时间计算；请求体中的任何 `duration` 字段被忽略；
- [ ] 无认证请求时接口不读客户端时间戳作为 `started_at`。

**排行榜**

- [ ] 总榜 / 月榜 / 周榜 / 今日榜数字与手工计算一致；
- [ ] 跨日、跨周、跨月会话被正确切分（含"整段时长不得算到开始日期"）；
- [ ] 活动会话按查询时刻计入；
- [ ] `invalid` 会话不计入；
- [ ] 禁用实例不出现在榜单中，历史数据仍可在后台查看；
- [ ] `limit > 200` 返回 400；`offset` 超限返回 400；
- [ ] 排序稳定（时长相同按 `install_id` 升序）；
- [ ] 时区固定为 `Asia/Shanghai` 计算自然日边界。

**版本管理**

- [ ] `GET /api/v1/releases/latest` 的响应能被现网 `remote_loader._validate_manifest` 校验通过（用真实代码跑一遍）；
- [ ] `GET /dist/latest.json` 与规范接口返回同一份清单体；
- [ ] `GET /dist/releases/{filename}` 返回 302 且 `Location` 指向 OSS 对象；
- [ ] 客户端附加的 `?v=123` 等未知查询参数不会导致 400；
- [ ] 校验失败的发布记录无法启用，也不会出现在 `latest` 中；
- [ ] `size > 20 MiB` 的发布记录入库即被拒绝；
- [ ] 渠道指针指向的版本被禁用时，接口不再返回它（指针拒绝失效写入）；
- [ ] 回滚 = 切换指针，且 `latest` 立即返回旧版本；
- [ ] 补录历史发布记录不会改变 `latest`；
- [ ] `update_required` 的计算正确（`forced` 与 `min_client_version` 组合，含版本逐段数值比较）；
- [ ] 查询已禁用版本返回 410 `RELEASE_DISABLED`；
- [ ] ZIP 文件不经过 ECS 中转（兼容路径只做 302）。

**安全与限流**

- [ ] 各接口限流阈值生效，超限返回 429 `RATE_LIMITED`；
- [ ] 请求体超限返回 413；
- [ ] `display_name` 含 `<script>` 之类内容时，管理页面渲染为纯文本（不执行、不破版）；
- [ ] 管理接口未登录返回 401，跨站写操作因 CSRF 校验失败返回 403；
- [ ] 发布、回滚、禁用、改名、禁用实例、手动结束会话全部产生操作日志（含 IP 与 `request_id`）；
- [ ] 错误响应统一为 `{error:{code,message,request_id}}`。

### 21.2 脚本接入验收（M6，独立立项）

- [ ] 首次运行生成 `install_id`，保存于 `KeyValue` 新键；
- [ ] 重新导入脚本、运行时更新、重启 AScript 后 `install_id` 不变；
- [ ] 重置表单缓存（`asdata`）不影响 `install_id`；
- [ ] 每次启动生成新的全局唯一 `run_id`；
- [ ] 心跳由独立线程发送，任务线程被看门狗强杀时心跳不受影响；
- [ ] 后台完全不可达时，脚本正常启动并执行任务（启动耗时增加 ≤ 3 秒）；
- [ ] 清单来源按"后台 → OSS → GitHub"顺序回退，且校验逻辑未放宽；
- [ ] `system.exit()` 强杀场景下，会话由后台按超时容差正确结算；
- [ ] 用户名可在脚本界面设置、修改，并即时反映到排行榜。

### 21.3 部署验收

- [ ] 干净目录 `docker compose up -d` 可启动；
- [ ] SQLite 数据在容器重建后仍然存在；
- [ ] backend / scheduler / caddy 异常后自动重启；
- [ ] 容器日志按 10MB × 3 轮转，不无限增长；
- [ ] 数据库可备份并**实际恢复演练成功一次**；
- [ ] HTTPS 正常访问，HTTP 自动跳转；
- [ ] `/healthz` 返回 200，数据库异常时返回 503；
- [ ] 迁移在启动时自动执行，且只有 backend 容器执行。

---

## 22. 后续扩展方向

第一阶段稳定后按实际需求扩展（扩展时保持当前接口兼容，不得改变 `install_id` 含义）：

1. 客户端接口鉴权升级（`X-Install-Secret`，见 14.3，向后兼容）；
2. 用户与多个安装实例绑定、身份迁移码；
3. 运行时灰度发布与按实例定向更新；
4. 版本使用统计与更新成功率；
5. 公告管理、远程配置；
6. 任务完成统计；
7. 可选的异常截图（走 OSS）；
8. 日汇总表与历史榜单快照；
9. 付费授权与订阅。

---

## 23. 最终开发原则

```text
客户端免费使用
普通用户暂时不登录
管理员必须登录
install_id 长期稳定，run_id 每次运行全局唯一
用户名只是显示属性，允许重名，不作为主键
时长由服务端时间计算，客户端不得提交时长
排行榜由服务端生成，统一现算口径
清单契约与现网设备兼容（schema=1 + package.*）
后台优先取清单，OSS/GitHub 必须保留兜底
OSS 负责大文件分发，ECS 只做元数据与重定向
SQLite + WAL 负责早期持久化，单 worker
Docker Compose 负责部署，scheduler 独立进程
契约先改文档与 OpenAPI 快照，再改代码
客户端网络失败不能阻塞脚本，启动路径超时 ≤ 3 秒
未做鉴权是显式决策，残余风险记录在案，升级路径保留
```

开发时优先保证**接口稳定、数据不丢失、统计结果正确**，再增加复杂的运营功能。

---

## 附录 A：契约文件位置

| 文件 | 作用 |
|---|---|
| `后台开发技术文档.md` | 本契约（语义与规则的唯一权威） |
| `docs/api/openapi-v1.yaml` | 客户端接口快照 |
| `docs/api/openapi-admin.yaml` | 管理后台接口快照 |
| `docs/archive/后台开发技术文档.v1.md` | v1 原文归档 |
| `AS-二重螺旋项目解析.md` | 脚本项目解析（脚本侧维护） |

快照自检（改完 OpenAPI 后必须跑一遍，确认可解析且没有悬空 `$ref`）：

```powershell
# 依赖 node + js-yaml（M1 起会改为 pytest 契约测试）
$env:NODE_PATH='<npx 缓存>\node_modules'
node -e "const y=require('js-yaml'),fs=require('fs');const d=y.load(fs.readFileSync(process.argv[1],'utf8'));const s=Object.keys((d.components||{}).schemas||{});const r=new Set();(function w(o){if(o&&typeof o==='object')for(const[k,v]of Object.entries(o))k==='$ref'?r.add(v):w(v);})(d);console.log('paths',Object.keys(d.paths).length,'schemas',s.length,'dangling',[...r].filter(x=>!s.includes(x.split('/').pop())).length)" "docs\api\openapi-v1.yaml"
```

## 附录 B：开发环境基线（本机，M1 起使用）

```text
解释器   D:\code_app\Anaconda\envs\erchong-backend\python.exe
         conda 环境 erchong-backend（独立创建，不复用任何工具的专用环境）
         创建命令：conda create -n erchong-backend python=3.12 pip -y
实测版本 Python 3.12.13 · pip 26.1.2 · SQLite 3.53.2 · zoneinfo(Asia/Shanghai) 正常

依赖（首次安装版本，M1 会用 requirements.txt 锁定）
         fastapi 0.141.1 · uvicorn 0.52.4 · sqlmodel 0.0.42（SQLAlchemy 2.0.52）
         alembic 1.20.0 · pydantic 2.13.5 · pydantic-settings 2.15.0
         argon2-cffi 25.1.0 · itsdangerous 2.2.0 · jinja2 3.1.6
         python-multipart 0.0.32 · httpx 0.28.1 · pytest 9.1.1 · freezegun 1.5.5 · tzdata 2026.4

调用方式 本机每次命令都是全新 shell，不使用 conda activate；一律用上面的绝对路径调用解释器，
         例如：& $PY -m pytest / & $PY -m uvicorn app.main:app --reload

容器     Dockerfile 固定 python:3.12-slim，与本地版本对齐
         （Debian 12 自带 SQLite 3.40，部分唯一索引 / UPSERT / VACUUM INTO / RETURNING 全部支持）

禁止     不要用 conda base（3.13）或 yingdao_3.10.11、bge（其他工具的专用环境，
         混装 pydantic-core 等会互相破坏依赖）
```

已在 SQLite 3.53 上实测通过：部分唯一索引（第 8.5 节）、`CHECK (status='active' OR ended_at IS NOT NULL)`、第 9.3 节 clamp 查询（`(1,1500) (2,500) (3,400)` 与手算一致）、`RETURNING`、`VACUUM INTO`（需在事务外）。

## 附录 C：v1 → v2 差异对照

| 章节 | v1 | v2 |
|---|---|---|
| 4 | 只列功能范围 | 增加 4.3 阶段边界与"后台阶段完成定义" |
| 5（新） | 无 | 事实基线：现网清单结构与客户端硬校验规则 |
| 6.1 | "具体持久化 API 待确认" | 明确 `KeyValue` 新键，列出禁止位置 |
| 6.2 | `run-20260912-001` 示例 | 全局唯一格式并给出正则 |
| 6.3 | 长度/控制字符限制 | 增加字符白名单、保留名、改名频率、转义要求 |
| 7.2 | 结束时间"按心跳或加容差"未定 | 定死 `last_heartbeat_at + 120s`，给出理由 |
| 7.3 | 幂等要求（原则） | 落到唯一索引、UPSERT、`MAX()`、`WHERE status='active'` |
| 7.4 | 建议结束旧会话 | 补部分唯一索引 + `BEGIN IMMEDIATE` 事务流程 |
| 8.1 | `package_url` / `package_size` 扁平结构 | 改为 `latest.json` 超集；新增 `/dist/*` 兼容路径 |
| 8.3 | `duration_seconds` 由后台计算 | 增加归属校验、409 语义、`next_heartbeat_seconds` |
| 8.5 | 含 `device_count` | 移除（恒为 1），增加 `total` / 分页与稳定排序 |
| 9.3 | "回滚到指定版本" | 渠道指针表 + 服务端完整性校验 |
| 10 | 表结构（时间未定类型） | 时间统一 INTEGER epoch 秒；新增字段与完整索引清单 |
| 10.6 | 汇总表"以后再说" | 明确触发汇总表的具体信号 |
| 11 | "要按日期切分" | clamp 算法 + 可执行 SQL + 时区边界 |
| 12 | 部署结构 | 单 worker、scheduler 独立、WAL、Alembic batch、备份恢复步骤 |
| 12.1 | 未指定管理后台前端实现 | 落定为 Jinja2 模板 + htmx + Bootstrap 5.3 CSS（全部 vendor 入库、不走 CDN、不引 Alpine）；页面路由与 JSON 接口共用 `app/services/`，业务规则只有一份 |
| 15 | 安全要求（原则） | 增加 14.3 已接受风险、Cookie/CSRF 方式、CSP、限流阈值表 |
| 20 | 骨架→DB→后台→接口→统计→安全 | M0 契约冻结→骨架→**会话核心**→发布与榜单→后台→运维 |
| 21 | 单张验收表（混两项目） | 拆为后台 / 脚本接入 / 部署三张 |
| 新 | 无 | 15 数据保留与容量、附录 A/B/C |

---

## 附录 D：实施记录（M1）

**范围**：项目骨架 + 数据库模型与迁移 + Compose 编排 + 健康检查。
**代码基线**：仓库首次提交（`git log` 首个 commit）。

### D.1 交付物

```text
app/main.py                      FastAPI 装配；中间件顺序：访问日志 → 请求 ID → 安全头 → 请求体上限
app/core/config.py               全部阈值集中配置（含"容差 < 超时"的关系校验）
app/core/db.py                   引擎 + PRAGMA（WAL / busy_timeout=5000 / synchronous=NORMAL / foreign_keys=ON）
app/core/errors.py               统一错误格式与 12 个错误码 ↔ HTTP 状态映射
app/core/middleware.py           X-Request-ID、安全头与 CSP、请求体上限、访问日志
app/core/timeutil.py             Unix 秒 ↔ ISO 8601、北京时间
app/core/storage.py              SQLite 路径处理（应用与迁移共用）
app/models/                      六张表：admins / installations / runtime_releases / release_channels
                                 / runtime_runs / admin_operation_logs
app/api/health.py                GET /healthz（200 / 503）
app/scheduler/__main__.py        独立定时任务进程（M1 仅自检与心跳日志）
alembic/                         迁移 20260912_0001（6 表 + 14 索引 + 全部 CHECK）
Dockerfile / docker/entrypoint.sh / docker-compose.yml / Caddyfile / .env.example
tests/                           27 个用例：健康检查、错误格式、安全头、表结构与约束行为
```

### D.2 实测验证（本机，SQLite 3.53）

| 项 | 结果 |
|---|---|
| `python -m pytest` | **27 passed** |
| `alembic upgrade head` | 建出 6 张业务表 + `alembic_version`，14 个索引 |
| 部分唯一索引 | `CREATE UNIQUE INDEX ux_runtime_runs_active_per_inst ON runtime_runs (installation_id) WHERE status = 'active'` |
| 约束行为 | 第二个活动会话、`stopped` 却无 `ended_at`、非法 status/channel、外键悬空、size > 20 MiB、sha256 长度 ≠ 64 —— 全部被数据库拒绝 |
| 迁移可逆 | 独立库上 `upgrade → downgrade base → upgrade` 通过 |
| `GET /healthz` | 200，`db=ok`、`migration=20260912_0001`、`server_time` 为 UTC |
| 迁移落后时 | 返回 503 `status=degraded`（用例覆盖） |
| 统一错误格式 | 404 → `NOT_FOUND`、413 → `INVALID_ARGUMENT`，均带 `request_id` |
| 安全头 | 响应带文档 12.4 的 CSP，且不含 `unsafe-inline` / `unsafe-eval` |
| 访问日志 | `GET /healthz 200 1.0ms request_id=req-…`，不记录请求体 |

### D.3 本阶段发现并已修正的问题

1. **`alembic.ini` 含中文会让迁移直接崩**（已修正 + 回归用例）。configparser 按 GBK 读取该文件，
   `alembic upgrade head` 抛 `UnicodeDecodeError` 且不显示文件名。已改为纯 ASCII 注释。
2. **SQLite 不自动创建数据库文件所在目录**（已修正）。缺目录只报 `unable to open database file`。
   新增 `app/core/storage.py`，应用启动与 `alembic/env.py` 都调用。
3. **`expected_revision()` 每次健康检查都读盘**（已优化为进程内缓存）。
4. **Caddyfile 的 `log` 指令不能放在 `handle` 块里**（已修正）。否则 Caddy 报
   `directive 'log' is not an ordered HTTP handler` 并陷入重启循环（实测）。
   `log` 只能放站点块或 `route` 块。
5. **`docker compose build` 在含中文的目录路径下必失败**（已用 ASCII 联接规避）。
   buildx 用「本地构建上下文路径」计算会话密钥，路径含非 ASCII 字符时报：
   `header key "x-docker-expose-session-sharedkey" contains value with non-printable ASCII characters`
   （[buildx#1684](https://github.com/docker/buildx/issues/1684)）。注意 **普通 `docker build` 不受影响**，
   只有 compose 走的 bake 路径会崩；实测把项目用 ASCII 联接（`D:\code\AI\as-erchong-admin`）
   暴露出来后 `docker compose build` 正常。已提供 `scripts/compose.ps1` 自动化这一步，
   项目目录本身不改名（改名会让当前工作区路径失效）。

### D.4 Docker 实测（2026-09-13，本机 Docker Desktop 4.90 / 引擎 29.7.2）

| 验证项 | 结果 |
|---|---|
| `docker compose build` | ✅ 从 ASCII 联接路径构建出 `erchong-admin:local`（294 MB） |
| 容器内迁移 | ✅ 日志 `Running upgrade -> 20260912_0001` + `数据库迁移完成` |
| backend 健康检查 | ✅ `Up (healthy)`，容器内 `/healthz` 200 |
| scheduler 启动顺序 | ✅ 先等待，拿到 `数据库就绪，迁移版本=20260912_0001` 后再进入循环 |
| 经 Caddy 访问 | ✅ `https://localhost/healthz` → 200（内部 CA，需 `curl -k`）；HTTP 308 跳转 HTTPS |
| 容器内 SQLite 参数 | ✅ 应用引擎连接：`foreign_keys=1`、`busy_timeout=5000`、`synchronous=1(NORMAL)`；文件为 `journal_mode=wal` |
| 卷内文件 | ✅ `/data/erchong.db` + `-wal` + `-shm`，属主 `appuser`（非 root 运行） |
| **容器重建后数据仍在** | ✅ 写入标记行 → `down` → `up -d` → 标记行与 `alembic_version` 均保留 |
| 数据盘位置 | ✅ Docker 数据盘已迁至 `D:\Docker\data\DockerDesktopWSL`（WSL 与镜像数据不再增长在 C 盘） |

### D.5 仍未验证 / 待办

- **ECS 上的真实部署**：2 核 2G 资源下的表现、域名 + 公网 HTTPS 证书签发、
  备份定时任务与**恢复演练**（文档 11.5 要求实际恢复一次）都还没做，属于 M5 范围。
- Windows 本地 `zoneinfo` 依赖 `tzdata` 包（已写入 `requirements.txt`）；容器基础镜像为 Debian，行为一致。

---

## 附录 E：实施记录（M2 会话核心）

**范围**：安装实例与会话的全部客户端接口、幂等与并发、超时扫描、异常数据保护、时长口径。
**验证**：81 个用例通过；假客户端经 Caddy → backend → SQLite 全链路跑通。

### E.1 交付物

```text
app/services/duration.py      区间边界（北京时间自然日/周/月）与 clamp 聚合，纯函数
app/services/identity.py      install_id / run_id / display_name 校验、注册、改名
app/services/session.py       开始 / 心跳 / 停止 / 超时扫描；_finalize_run 是唯一结束出口
app/core/db.py                immediate_transaction（BEGIN IMMEDIATE）与会话工厂
app/api/deps.py               客户端 IP、请求体上限依赖
app/api/v1/installations.py   POST /register、GET /{id}、PATCH /{id}/profile
app/api/v1/runs.py            POST /runs、POST /runs/{id}/heartbeat、POST /runs/{id}/stop
app/api/v1/schemas.py         请求/响应模型（与 openapi-v1.yaml 逐字对齐）
app/scheduler/__main__.py     接入会话超时扫描（每分钟）
scripts/fake_client.py        端到端假客户端（含三项幂等检查）
tests/test_duration.py        区间边界与 clamp
tests/test_installations.py   注册幂等、显示名校验、禁用实例、限流上限
tests/test_runs.py            开始/心跳/停止、幂等、归属校验、并发替换
tests/test_timeout_scan.py    超时结算、异常数据保护
```

### E.2 关键实现决定

1. **超时结算与异常保护收敛到一处**：`_finalize_run()` 是停止、被替换、超时扫描三条
   路径的唯一出口，异常数据保护（文档 8.7）只写一遍，不会漏。
2. **`BEGIN IMMEDIATE` 而不是"先查后写"**：开始会话时立刻取写锁，配合 `busy_timeout=5000`，
   并发请求会串行执行而不是双双读到"没有活动会话"。
3. **请求体上限放在中间件而不是路由依赖**：FastAPI 先解析请求体再执行依赖，
   超大的非法 JSON 会在依赖运行前以 400 失败——实测踩到过，见 E.4。
4. **不可信时间**：`started_at` 只取服务端时间；心跳用 `max()` 写回，乱序/重放都不会回退。

### E.3 实测结果

| 项 | 结果 |
|---|---|
| `pytest` | **81 passed** |
| 假客户端（经 Caddy） | 注册 200 → 重复注册 200（显示名未被清空）→ 开始 201 → 重复开始 200（`started_at` 未变）→ 心跳 200 → 停止 200 → 重复停止 200（`ended_at`/`end_reason` 未变）→ 结束后心跳 409 → 档案 200 |
| 并发开始会话 | 两个线程同时开始不同 `run_id`：最终恰好一个活动会话，另一个被标记 `replaced_by_new_run` |
| 超时扫描（容器内实跑） | 掉线 700 秒的会话被关闭：`status=timeout`、`end_reason=heartbeat_timeout`、`ended_at - last_heartbeat = 120 秒` |
| 异常数据保护 | 开始时间在未来 → `invalid`/时长 0；超过 30 天 → 时长截断到上限并标 `invalid` |
| 累计时长口径 | 只统计 `active`/`stopped`/`timeout`，`invalid` 不计入；活动会话按查询时刻计入 |

### E.4 本阶段发现并已修正的问题

1. **两条测试在"假通过"**：清理夹具（每个用例前清空业务表）上线后暴露出来——
   - `test_release_verified_status_enum` 之前是靠上一个用例留下的 `release_id` 触发唯一索引冲突
     才"通过"的，改的列根本没进 INSERT 语句；
   - 会话心跳用例读的是同一 Session 的身份映射缓存对象，接口更新后断言到的还是旧值。
   两条都已按真实行为重写（`session.expire_all()` + 显式列出所有被断言的列）。
2. **per-route 请求体上限放错了层**：写在路由依赖里时，超大且非法的 JSON 会先被
   FastAPI 解析报 400，上限检查永远轮不到。已移到中间件按路径判断（E.2 第 3 条）。
3. **`.ps1` 被本机执行策略拦住**：新增 `scripts/compose.cmd` 包装（`-ExecutionPolicy Bypass`
   只对本次调用放行，不改系统设置）。
4. **`.cmd` 里的中文注释会让脚本崩**：cmd.exe 按 OEM 代码页读取批处理文件，中文字节被
   误判成命令分隔符。批处理文件已改为纯 ASCII——与 `alembic.ini`、`~/.ssh/config` 同一个病根。
5. **本机 httpx 会走系统代理**：Windows 系统代理开启时，httpx 经 `urllib.request.getproxies()`
   读到注册表代理，却不读注册表里写着 `localhost` 的 bypass 名单，本地自签证书握手被打断。
   `scripts/fake_client.py` 已默认 `trust_env=False`。
6. **裸 `sqlite3` 连接不继承应用 PRAGMA**：排障脚本里 `foreign_keys` 默认是关的，
   删实例会留下孤儿会话行（已清理并在 README 记录）。

---

## 附录 F：实施记录（M3 发布与排行榜）

**范围**：清单接口（含零改动兼容路径）、发布入库的服务端完整性校验、渠道指针与回滚、排行榜。
**验证**：127 个用例通过；并用**现网客户端的真实代码**跑通了完整更新链路。

### F.1 交付物

```text
app/services/releases.py     发布记录、版本比较、服务端下载校验、渠道指针、清单构造
app/services/ranking.py      区间取数、聚合、排序分页、公开标识掩码
app/api/v1/releases.py       GET /api/v1/releases/latest、GET /api/v1/releases/{release_id}
app/api/v1/rankings.py       GET /api/v1/rankings/runtime-duration
app/api/compat.py            GET /dist/latest.json、GET /dist/releases/{filename}（302 → OSS）
app/scheduler/__main__.py    接入发布校验重试（每 5 分钟）
tests/test_releases.py       清单超集、渠道解析、校验状态机、指针规则、兼容路径
tests/test_rankings.py       聚合口径、排序分页、掩码、参数校验
tests/test_manifest_contract.py  用真实 remote_loader 校验清单（含反向用例）
```

### F.2 实测结果（真实数据，非构造）

| 验证项 | 结果 |
|---|---|
| 服务端发布校验 | 登记真实发布包 `v2.0.0.8-94503c6d0762` 后，后台自行从 OSS 下载并核对：`verified_status=ok` |
| 清单通过真实客户端校验 | 加载脚本项目的 `remote_loader._validate_manifest`，直接校验后台 `/dist/latest.json` → 通过 |
| 零改动下载链路 | 用设备同款库 `requests`，按 `_download` 的写法请求后台兼容路径 → 302 → OSS → 696613 字节，SHA-256 与清单一致 |
| 破缓存参数 | `/dist/latest.json?v=12345678` 正常返回（客户端会附加该参数） |
| 未知包名 | `/dist/releases/anything-else.zip` → 404（防开放重定向） |
| 排行榜 | 经 Caddy 查询：`period=all` 与 `today` 均正确；`today` 的起点为 `2026-09-12T16:00:00Z`，正是北京时间 0 点；`limit=201` → 400 |
| 契约测试 | `pytest` **127 passed**（其中 6 条为加载真实客户端代码的契约测试，非跳过） |

### F.3 关键实现决定

1. **清单是 `latest.json` 的超集，不是替代结构**：`schema` / `release_id` / `app_version` /
   `created_at` / `package.{path,sha256,size}` 逐字保持原样，扩展字段客户端会忽略（文档 7.2）。
   `tests/test_manifest_contract.py` 还会拿仓库里真实发布的 `dist/latest.json` 比对字段集合，
   防止以后"优化"清单结构时把兼容面改坏。
2. **零改动接入路径已实测可用**：脚本端只需把后台基址插到 `SOURCE_BASES` 首位，
   下载与校验逻辑一行不改（文档 7.3）。
3. **发布记录先入库、后校验、再启用**：`enabled=0` + `verified_status=unverified` 起步，
   服务端下载比对通过才允许启用；重新校验失败会自动把已启用的版本禁用——宁可停更也不发坏包。
4. **禁用正被渠道指针使用的版本返回 409**，必须先切指针：否则该渠道会直接没有可用版本。
5. **榜单聚合复用同一份纯函数**：数据库侧只做 `WHERE` 过滤，clamp 与求和走
   `app/services/duration.py`（口径与文档 9.3 的 SQL 完全一致，但只有一处实现）。

### F.4 本阶段发现并已修正的问题

1. **`httpx` 默认不跟随重定向**（`follow_redirects=False`），而脚本端用的 `requests`
   默认跟随。这个差异会直接决定"302 兼容路径到底能不能用"——第一次验证时脚本按 httpx
   的默认写，下到 0 字节，换成 `requests`（设备端真实调用方式）后完整跑通。
   **脚本端保持用 `requests` 就没问题**；M6 接入时如果哪天换成 httpx，必须显式打开
   `follow_redirects`，否则设备会拿到一个空的 302 响应体。
2. **真实客户端代码依赖 `requests`**：契约测试要加载 `remote_loader.py`，因此把 `requests`
   放进 `requirements-dev.txt`（仅测试用，运行时仍用 httpx）。
3. **`app/api/compat.py` 的相对导入写多了一层**：`app.api` 包内应为 `..core`，写成 `...core`
   会抛 `attempted relative import beyond top-level package`。同类问题在 M2 也出现过一次，
   规律是"包层级决定点数"，新增模块时容易数错。

---

## 附录 G：实施记录（M4 管理后台）

**范围**：管理员登录/会话/CSRF、七个管理页面、管理端 JSON 接口、审计日志、运行时可改设置。
**验证**：180 个用例通过；本机 Docker 全链路走查通过。

### G.1 交付物

```text
app/services/admin_auth.py     Argon2id 密码、签名会话 Cookie、登录锁定、引导管理员、改密
app/services/audit.py          审计动作与落库（含中文动作名）
app/services/settings_store.py 运行时可改设置（数据库 > 环境变量），带类型校验
app/services/admin_reports.py  概览统计、分页列表、实例/版本使用情况、渠道变更历史
app/admin/deps.py              模板环境（含 epoch/duration/filesize 过滤器）、会话与 CSRF 依赖
app/admin/pages.py             登录、概览、版本、实例、会话、排行榜、日志、设置（含 htmx 片段）
app/admin/api.py               管理端 JSON 接口（字段逐字对齐冻结快照）
app/admin/templates/           17 个模板（base/macros/各页面 + 4 个 htmx 片段 + 登录/改密/错误页）
app/admin/static/vendor/       本地 htmx 2.x + Bootstrap 5.3 CSS（无 CDN，附版本与许可证）
alembic/versions/20260913_0002 新增 runtime_settings 表与 admins.must_change_password
tests/test_admin_auth.py       登录、锁定、会话、CSRF、改密、页面跳转
tests/test_admin_pages.py      页面渲染、发布全流程、实例/会话操作、设置生效
tests/test_admin_contract.py   管理端接口与冻结快照的字段级一致性
```

### G.2 实测结果（本机 Docker，经 Caddy）

| 验证项 | 结果 |
|---|---|
| 迁移 0002 | `Running upgrade 20260912_0001 -> 20260913_0002`，新增表与列均落库 |
| 引导管理员 | 首次启动按环境变量创建，日志只记用户名不记密码 |
| 强制改密 | 未改密访问 `/admin/` → 303 跳 `/admin/password`；改密后 `bootstrap_done=true` |
| CSRF | 页面写操作缺令牌 → 403 且页面提示含 CSRF |
| 七个页面 | 概览/版本/实例/会话/排行榜/日志/设置 全部 200（5～15 KB） |
| 契约一致性 | 管理端接口响应与冻结快照**逐字段**一致（少字段、多字段都会红） |
| 本地资源 | `/admin/static/vendor/htmx.min.js` 200；页面**不含任何 CDN 引用** |
| 回归 | 客户端清单 `/dist/latest.json` 与真实发布包链路不受影响 |

### G.3 关键实现决定

1. **无状态会话 + 双重提交 CSRF**：会话是签名 Cookie（内含 CSRF 令牌），不落库；
   空闲超时用 Cookie 自身的 `max_age`（每次请求重签），绝对超时用载荷里的 `iat` 兜底。
2. **页面与接口共用 service**：`pages.py` 与 `api.py` 都只做解析/渲染，业务规则
   （发布状态机、时长口径、字段校验）只有一份实现（文档 12.1）。
3. **异常处理器分叉而非各写一套**：`app/admin/__init__.py` 注册的 `ApiError` 处理器
   按路径分流——`/admin/*` 页面渲染错误页，其余路径原样退回 JSON，保证 `/api/v1`
   与 `/dist/*` 的错误格式不被改坏。
4. **管理端接口拿冻结快照当验收标准**：`tests/test_admin_contract.py` 从
   `docs/api/openapi-admin.yaml` 读 schema，比对真实响应的字段集合。写实现时先按直觉
   命名，再被这个测试逼着逐条对齐——这正是把快照冻结下来的价值。

### G.4 本阶段发现并已修正的问题

1. **`context=admin_context` 传的是函数而不是依赖**：FastAPI 只把 `Depends(...)` 当依赖，
   直接写函数名会让参数收到函数对象，报 `'function' object has no attribute 'admin'`。
   统一改用 `AdminDep = Depends(admin_context)`。
2. **改了 `alembic/` 却没重建镜像**：compose 的开发用绑定挂载只覆盖 `./app`，
   `alembic/` 是构建时 COPY 进镜像的。新增迁移后只 `restart` 会让容器跑旧迁移文件，
   现象是"日志说迁移完成、库里却没有新表"，而且引导管理员创建失败只留下一条
   ERROR 日志（被"不影响启动"的兜底吞掉）。**改迁移必须重新 build**。
3. **裸 SQL 写入设置表要用 UPSERT**：scheduler 记录心跳用的是
   `INSERT ... ON CONFLICT(key) DO UPDATE`，避免先查后写的竞态。

